"""工程能力指数 Cp・Cpk の計算。"""
from ._spec import Affiliate, Faq, Field, Output, Tool

ASP = "※ リンク先は提携先のサイトです。導入前に自社の要件をご確認ください。"

TOOL = Tool(
    slug="cpk",
    title="工程能力指数 Cp・Cpk 計算ツール",
    description="規格上限・下限と、実測の平均・標準偏差から、工程能力指数 Cp と Cpk、かたよりの度合いを計算します。",
    category="品質管理",
    lead="Cp は規格幅に対するばらつきの小ささ、Cpk はそこに中心のずれを加味した指標です。"
         "Cp が高いのに Cpk が低い場合、ばらつきではなく中心がずれています。"
         "その切り分けができると、打つ手が変わります。",
    updated="2026-08-29",
    keywords=["Cpk 計算", "工程能力指数", "Cp Cpk 違い", "工程能力指数 求め方"],
    inputs=[
        Field("usl", "規格上限（USL）", unit="", default=10.5,
              hint="図面や仕様書の上限値"),
        Field("lsl", "規格下限（LSL）", unit="", default=9.5),
        Field("mean", "実測の平均", unit="", default=10.1,
              hint="測定データの平均値"),
        Field("sigma", "実測の標準偏差", unit="", default=0.12, min=0,
              hint="測定データの標準偏差。表計算のSTDEV関数で出せます"),
    ],
    outputs=[
        Output("cpk", "Cpk（かたよりを含む工程能力）",
               "Math.min((usl - mean) / (3 * sigma), (mean - lsl) / (3 * sigma))",
               unit="", decimals=2, primary=True,
               note="1.33以上が一般的な合格ライン"),
        Output("cp", "Cp（ばらつきだけを見た工程能力）",
               "(usl - lsl) / (6 * sigma)", unit="", decimals=2,
               note="中心がずれていても下がりません"),
        Output("k", "K（かたより度）",
               "Math.abs((usl + lsl) / 2 - mean) / ((usl - lsl) / 2)",
               unit="", decimals=3,
               note="0なら中心。大きいほど規格の片側に寄っています"),
        Output("center", "規格の中心", "(usl + lsl) / 2", unit="", decimals=3),
        Output("shift", "中心からのずれ", "mean - (usl + lsl) / 2", unit="", decimals=3,
               note="正なら上側、負なら下側にずれています"),
        Output("margin_upper", "上限までの余裕", "(usl - mean) / sigma",
               unit="σ", decimals=2),
        Output("margin_lower", "下限までの余裕", "(mean - lsl) / sigma",
               unit="σ", decimals=2),
    ],
    formula_note="""Cp  ＝ (規格上限 − 規格下限) ÷ (6 × 標準偏差)
Cpk ＝ min( (規格上限 − 平均) ÷ 3σ , (平均 − 規格下限) ÷ 3σ )
K   ＝ |規格の中心 − 平均| ÷ (規格幅 ÷ 2)

Cp は「ばらつきの小ささ」だけを見ます。中心がどれだけずれていても変わりません。
Cpk はそこに中心のずれを加味するので、Cp ＞ Cpk のとき差の分だけ中心がずれています。

Cp が十分あるのに Cpk が低いなら、ばらつきを減らす改善ではなく
中心を規格中央に合わせる調整が先です。段取りや刃具の初期設定で直ることが多い項目です。""",
    steps=[
        "図面・仕様書から規格上限と下限を入れます。",
        "実測データの平均と標準偏差を入れます。表計算のAVERAGE・STDEV関数で出せます。",
        "Cp と Cpk を比べます。差が大きければ中心のずれが原因です。",
        "上限・下限それぞれの余裕（σ単位）を見て、どちら側に寄っているか確認します。",
    ],
    faq=[
        Faq("Cpk はいくつあれば良いですか",
            "1.33以上を合格ラインとする例が多く、重要特性では1.67以上を求める場合もあります。"
            "取引先の要求水準が定められていることが多いので、まずそちらを確認してください。"),
        Faq("Cp と Cpk の差は何を意味しますか",
            "中心のずれです。Cp は規格幅とばらつきだけで決まり、"
            "Cpk はそこに中心位置を反映します。両者が近いほど中心が合っています。"),
        Faq("片側規格しかない場合は",
            "このツールは両側規格を前提にしています。片側しかない場合は、"
            "余裕のある側だけの計算（上限のみなら (USL − 平均) ÷ 3σ）を使ってください。"
            "上限・下限の余裕をσ単位で出しているので、そちらが参考になります。"),
        Faq("データは何個必要ですか",
            "一般に最低30個、できれば50個以上が目安とされます。"
            "少ないデータで出した標準偏差は信頼できず、Cpk も当てになりません。"),
    ],
    affiliate=Affiliate(
        heading="工程能力の監視を自動化する",
        body="Cpk は一度出して終わりではなく、日々の測定データから継続して監視するものです。"
             "品質管理システムを使うと、測定値の入力から管理図・工程能力指数の算出までが"
             "自動で回り、規格外の傾向を早い段階で拾えます。",
        cta="品質管理システムを比較する",
        url="https://example.com/",
        note=ASP,
    ),
)
