# このパッケージが読む環境変数は無い。
#
# 株探からの取得は引数と設定で完結する。API キーの類も使わない。
# 「.env が要るのに例が無いのか」を毎回確かめずに済むよう、
# 無いことを明示するためだけに置いてある。
#
# 取得そのものを CI から回さないこと（GitHub Actions の IP は 405 で弾かれる）。
# 取得はこのPCのタスク kabu-daily-fetch が行う。
