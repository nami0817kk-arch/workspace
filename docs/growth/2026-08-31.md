# 成長ループ ダイジェスト 2026-08-31

対象 13 プロジェクト / 平均成熟度 **54** / 未対応 52 件

## 今回の推奨アクション

### 1. [高] 定期実行ジョブが黙って失敗する状態になっている

- 対象: `nami0817kk-arch/kabu-agari-ranking`
- 種別: 自動化 / 2回目
- なぜ: スケジュール実行は、失敗しても手元で気づく機会がない。データ更新が止まっていることに何日も後で気づく、という壊れ方をする。
- やること: ワークフロー末尾に `if: failure()` の通知ステップを足す（Issue 起票 / メール / Webhook のいずれか）。GitHub は定期ジョブ失敗を通知しない設定のこともあるので、明示的に鳴らす。
- 根拠: `.github/workflows/daily-gainers-site.yml`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/kabu-agari-ranking の「定期実行ジョブが黙って失敗する状態になっている」に対応してください。

背景: スケジュール実行は、失敗しても手元で気づく機会がない。データ更新が止まっていることに何日も後で気づく、という壊れ方をする。
やること: ワークフロー末尾に `if: failure()` の通知ステップを足す（Issue 起票 / メール / Webhook のいずれか）。GitHub は定期ジョブ失敗を通知しない設定のこともあるので、明示的に鳴らす。
該当: .github/workflows/daily-gainers-site.yml

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze e7926b63dce1 -n "理由"`
今後も出さない: `python -m growth dismiss e7926b63dce1 -n "理由"`
</details>

### 2. [高] 環境変数を使っているのに .gitignore が .env を除外していない

- 対象: `nami0817kk-arch/kabu-agari-ranking`
- 種別: セキュリティ / 2回目
- なぜ: ローカルで `.env` を作った瞬間に、うっかりコミットする事故が起きる。
- やること: `.gitignore` に `.env` を追加する。1行で終わる割に事故の期待値が大きい。

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/kabu-agari-ranking の「環境変数を使っているのに .gitignore が .env を除外していない」に対応してください。

背景: ローカルで `.env` を作った瞬間に、うっかりコミットする事故が起きる。
やること: `.gitignore` に `.env` を追加する。1行で終わる割に事故の期待値が大きい。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze f9fed0c9fb2b -n "理由"`
今後も出さない: `python -m growth dismiss f9fed0c9fb2b -n "理由"`
</details>

### 3. [高] 自動テストが1本もない

- 対象: `nami0817kk-arch/kabu-agari-ranking`
- 種別: 動作の確からしさ / 2回目
- なぜ: コードが 4 ファイル / 約 537 行ある。この規模だと手で全部確かめるのは無理で、壊れたことに気づくのが本番になる。
- やること: まず一番壊れて困る関数1つに対してテストを1本書く。網羅は狙わない。`tests/test_<対象>.py` を作り、正常系1件・異常系1件から始める。
参考: soccer-manager が既に同じことをやっているので、そこから写すのが早い。
- 根拠: `お手本: nami0817kk-arch/soccer-manager`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/kabu-agari-ranking の「自動テストが1本もない」に対応してください。

背景: コードが 4 ファイル / 約 537 行ある。この規模だと手で全部確かめるのは無理で、壊れたことに気づくのが本番になる。
やること: まず一番壊れて困る関数1つに対してテストを1本書く。網羅は狙わない。`tests/test_<対象>.py` を作り、正常系1件・異常系1件から始める。
参考: soccer-manager が既に同じことをやっているので、そこから写すのが早い。
お手本: soccer-manager の実装に合わせる。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 1e6f4f6cbdca -n "理由"`
今後も出さない: `python -m growth dismiss 1e6f4f6cbdca -n "理由"`
</details>

### 4. [高] 依存パッケージがどこにも書かれていない

- 対象: `nami0817kk-arch/price-tracker`
- 種別: 動作の確からしさ
- なぜ: 別マシンや CI で動かすときに、何を入れればいいか誰も分からない。
- やること: `requirements.txt`（または `pyproject.toml`）を作り、import しているサードパーティを列挙する。

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/price-tracker の「依存パッケージがどこにも書かれていない」に対応してください。

背景: 別マシンや CI で動かすときに、何を入れればいいか誰も分からない。
やること: `requirements.txt`（または `pyproject.toml`）を作り、import しているサードパーティを列挙する。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze d562584e6f6a -n "理由"`
今後も出さない: `python -m growth dismiss d562584e6f6a -n "理由"`
</details>

### 5. [高] 環境変数を使っているのに .gitignore が .env を除外していない

- 対象: `nami0817kk-arch/price-tracker`
- 種別: セキュリティ
- なぜ: ローカルで `.env` を作った瞬間に、うっかりコミットする事故が起きる。
- やること: `.gitignore` に `.env` を追加する。1行で終わる割に事故の期待値が大きい。

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/price-tracker の「環境変数を使っているのに .gitignore が .env を除外していない」に対応してください。

背景: ローカルで `.env` を作った瞬間に、うっかりコミットする事故が起きる。
やること: `.gitignore` に `.env` を追加する。1行で終わる割に事故の期待値が大きい。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 405783832f52 -n "理由"`
今後も出さない: `python -m growth dismiss 405783832f52 -n "理由"`
</details>

### 6. [高] 依存パッケージがどこにも書かれていない

- 対象: `nami0817kk-arch/tool-factory`
- 種別: 動作の確からしさ
- なぜ: 別マシンや CI で動かすときに、何を入れればいいか誰も分からない。
- やること: `requirements.txt`（または `pyproject.toml`）を作り、import しているサードパーティを列挙する。

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/tool-factory の「依存パッケージがどこにも書かれていない」に対応してください。

背景: 別マシンや CI で動かすときに、何を入れればいいか誰も分からない。
やること: `requirements.txt`（または `pyproject.toml`）を作り、import しているサードパーティを列挙する。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 559d9a3b3e0d -n "理由"`
今後も出さない: `python -m growth dismiss 559d9a3b3e0d -n "理由"`
</details>

### 7. [高] README が無い

- 対象: `nami0817kk-arch/ir-analysis`
- 種別: ドキュメント
- なぜ: 何のプロジェクトで、どう動かすのかが分からない。数ヶ月後の自分が一番困る。
- やること: README.md に「目的」「動かし方」「構成」の3節を書く。長さは要らない。
参考: soccer-manager が既に同じことをやっているので、そこから写すのが早い。
- 根拠: `お手本: nami0817kk-arch/soccer-manager`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/ir-analysis の「README が無い」に対応してください。

背景: 何のプロジェクトで、どう動かすのかが分からない。数ヶ月後の自分が一番困る。
やること: README.md に「目的」「動かし方」「構成」の3節を書く。長さは要らない。
参考: soccer-manager が既に同じことをやっているので、そこから写すのが早い。
お手本: soccer-manager の実装に合わせる。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze da558a530e63 -n "理由"`
今後も出さない: `python -m growth dismiss da558a530e63 -n "理由"`
</details>

### 8. [高] 自動テストが1本もない

- 対象: `nami0817kk-arch/ir-analysis`
- 種別: 動作の確からしさ
- なぜ: コードが 8 ファイル / 約 496 行ある。この規模だと手で全部確かめるのは無理で、壊れたことに気づくのが本番になる。
- やること: まず一番壊れて困る関数1つに対してテストを1本書く。網羅は狙わない。`tests/test_<対象>.py` を作り、正常系1件・異常系1件から始める。
参考: soccer-manager が既に同じことをやっているので、そこから写すのが早い。
- 根拠: `お手本: nami0817kk-arch/soccer-manager`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/ir-analysis の「自動テストが1本もない」に対応してください。

背景: コードが 8 ファイル / 約 496 行ある。この規模だと手で全部確かめるのは無理で、壊れたことに気づくのが本番になる。
やること: まず一番壊れて困る関数1つに対してテストを1本書く。網羅は狙わない。`tests/test_<対象>.py` を作り、正常系1件・異常系1件から始める。
参考: soccer-manager が既に同じことをやっているので、そこから写すのが早い。
お手本: soccer-manager の実装に合わせる。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 21cd171bacf3 -n "理由"`
今後も出さない: `python -m growth dismiss 21cd171bacf3 -n "理由"`
</details>

### 9. [高] README が無い

- 対象: `nami0817kk-arch/quality-gainer-tracker`
- 種別: ドキュメント
- なぜ: 何のプロジェクトで、どう動かすのかが分からない。数ヶ月後の自分が一番困る。
- やること: README.md に「目的」「動かし方」「構成」の3節を書く。長さは要らない。
参考: soccer-manager が既に同じことをやっているので、そこから写すのが早い。
- 根拠: `お手本: nami0817kk-arch/soccer-manager`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/quality-gainer-tracker の「README が無い」に対応してください。

背景: 何のプロジェクトで、どう動かすのかが分からない。数ヶ月後の自分が一番困る。
やること: README.md に「目的」「動かし方」「構成」の3節を書く。長さは要らない。
参考: soccer-manager が既に同じことをやっているので、そこから写すのが早い。
お手本: soccer-manager の実装に合わせる。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze f8e69ac61ca3 -n "理由"`
今後も出さない: `python -m growth dismiss f8e69ac61ca3 -n "理由"`
</details>

### 10. [高] 自動テストが1本もない

- 対象: `nami0817kk-arch/quality-gainer-tracker`
- 種別: 動作の確からしさ
- なぜ: コードが 12 ファイル / 約 1348 行ある。この規模だと手で全部確かめるのは無理で、壊れたことに気づくのが本番になる。
- やること: まず一番壊れて困る関数1つに対してテストを1本書く。網羅は狙わない。`tests/test_<対象>.py` を作り、正常系1件・異常系1件から始める。
参考: soccer-manager が既に同じことをやっているので、そこから写すのが早い。
- 根拠: `お手本: nami0817kk-arch/soccer-manager`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/quality-gainer-tracker の「自動テストが1本もない」に対応してください。

背景: コードが 12 ファイル / 約 1348 行ある。この規模だと手で全部確かめるのは無理で、壊れたことに気づくのが本番になる。
やること: まず一番壊れて困る関数1つに対してテストを1本書く。網羅は狙わない。`tests/test_<対象>.py` を作り、正常系1件・異常系1件から始める。
参考: soccer-manager が既に同じことをやっているので、そこから写すのが早い。
お手本: soccer-manager の実装に合わせる。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 08001832df44 -n "理由"`
今後も出さない: `python -m growth dismiss 08001832df44 -n "理由"`
</details>

### 11. [高] README が無い

- 対象: `nami0817kk-arch/cohabitation-budget`
- 種別: ドキュメント
- なぜ: 何のプロジェクトで、どう動かすのかが分からない。数ヶ月後の自分が一番困る。
- やること: README.md に「目的」「動かし方」「構成」の3節を書く。長さは要らない。

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/cohabitation-budget の「README が無い」に対応してください。

背景: 何のプロジェクトで、どう動かすのかが分からない。数ヶ月後の自分が一番困る。
やること: README.md に「目的」「動かし方」「構成」の3節を書く。長さは要らない。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 1a887352b847 -n "理由"`
今後も出さない: `python -m growth dismiss 1a887352b847 -n "理由"`
</details>

### 12. [中] CI が無く、push しても誰も検証していない

- 対象: `nami0817kk-arch/price-tracker`
- 種別: 自動化
- なぜ: 壊れたコードが master に入っても気づけない。
- やること: `.github/workflows/ci.yml` を追加し、push と pull_request で依存インストール + テスト（または最低限 import チェック）を走らせる。
参考: soccer-manager が既に同じことをやっているので、そこから写すのが早い。
- 根拠: `お手本: nami0817kk-arch/soccer-manager`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/price-tracker の「CI が無く、push しても誰も検証していない」に対応してください。

背景: 壊れたコードが master に入っても気づけない。
やること: `.github/workflows/ci.yml` を追加し、push と pull_request で依存インストール + テスト（または最低限 import チェック）を走らせる。
参考: soccer-manager が既に同じことをやっているので、そこから写すのが早い。
お手本: soccer-manager の実装に合わせる。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 33abedcb0a56 -n "理由"`
今後も出さない: `python -m growth dismiss 33abedcb0a56 -n "理由"`
</details>

## あなたの判断が要るもの

これは作業ではなく決めごと。決まるまで私の側では動かせない。

- `kabu-agari-ranking` **公開リポジトリにライセンスが無い** — 意図に合う LICENSE ファイルを置く。自由に使ってよいなら MIT、そうでないなら README に条件を明記する。
- `price-tracker` **公開リポジトリにライセンスが無い** — 意図に合う LICENSE ファイルを置く。自由に使ってよいなら MIT、そうでないなら README に条件を明記する。
- `tool-factory` **公開リポジトリにライセンスが無い** — 意図に合う LICENSE ファイルを置く。自由に使ってよいなら MIT、そうでないなら README に条件を明記する。
- `claude-code-dev` **公開リポジトリにライセンスが無い** — 意図に合う LICENSE ファイルを置く。自由に使ってよいなら MIT、そうでないなら README に条件を明記する。
- `cohabitation-budget` **公開リポジトリにライセンスが無い** — 意図に合う LICENSE ファイルを置く。自由に使ってよいなら MIT、そうでないなら README に条件を明記する。
- `claude-code-dev/projects/PJT002-ai-blog` **雛形だけ作られて中身が無い** — 次の一手を1つだけ決めて README に書く（例:「最初に作る機能はこれ」）。当面やらないなら README にその旨を書くか、リポジトリをアーカイブする。
- `youtube-video-creation` **雛形だけ作られて中身が無い** — 次の一手を1つだけ決めて README に書く（例:「最初に作る機能はこれ」）。当面やらないなら README にその旨を書くか、リポジトリをアーカイブする。
- `cohabitation-budget` **雛形だけ作られて中身が無い** — 次の一手を1つだけ決めて README に書く（例:「最初に作る機能はこれ」）。当面やらないなら README にその旨を書くか、リポジトリをアーカイブする。

## 保留中（理由あり）

見たうえで今はやらないと判断したもの。状況が変われば `growth run` の次回で戻せる。

- `claude-code-dev/projects/PJT001-stock-investment` 依存バージョンが固定されていない — pywin32 を含むため Windows 側で pip freeze を取る必要がある。検証できないまま版を固定すると、動いている環境をかえって壊す
- `claude-code-dev/projects/PJT001-stock-investment` 1つの関数が長くなりすぎている — selector.py はテストが1本も無く、ネットワークに出る処理も多い。screener.py のように挙動を固定してからでないと切り出せない
- `youtube-video-creation` tests/ が空のまま置かれている — テスト対象のコードがまだ無い。最初に作る機能が決まってから

## 見送った分

1回あたりの提案数の上限に達したため、40 件は次回以降に回した。

- `kabu-agari-ranking` ワークフローに実行時間の上限が無い（中）
- `kabu-agari-ranking` 依存バージョンが固定されていない（中）
- `kabu-agari-ranking` 必要な環境変数の一覧が無い（中）
- `kabu-agari-ranking` 例外を握りつぶしている箇所がある（中）
- `kabu-agari-ranking` CLAUDE.md で AI に前提を渡す（サッカークラブ経営シミュレーション（Flutter） で既に実践中）（中）
- `price-tracker` 必要な環境変数の一覧が無い（中）
- `price-tracker` CLAUDE.md で AI に前提を渡す（サッカークラブ経営シミュレーション（Flutter） で既に実践中）（中）
- `soccer-manager` ワークフローに実行時間の上限が無い（中）
- `tool-factory` CI が無く、push しても誰も検証していない（中）
- `tool-factory` CLAUDE.md で AI に前提を渡す（サッカークラブ経営シミュレーション（Flutter） で既に実践中）（中）
- `ai-lab` 依存バージョンが固定されていない（中）
- `ai-lab` 1つの関数が長くなりすぎている（中）
- `ai-side-business` CI が無く、push しても誰も検証していない（中）
- `ai-side-business` 依存バージョンが固定されていない（中）
- `ai-side-business` 1つの関数が長くなりすぎている（中）
- `ai-side-business` CLAUDE.md で AI に前提を渡す（サッカークラブ経営シミュレーション（Flutter） で既に実践中）（中）
- `claude-code-dev/projects/PJT001-stock-investment` 例外を握りつぶしている箇所がある（中）
- `ir-analysis` CI が無く、push しても誰も検証していない（中）
- `ir-analysis` 依存バージョンが固定されていない（中）
- `ir-analysis` CLAUDE.md で AI に前提を渡す（サッカークラブ経営シミュレーション（Flutter） で既に実践中）（中）

## プロジェクト別の成熟度

| プロジェクト | 種別 | 成熟度 | 前回比 | 未対応 |
|---|---|---:|---:|---:|
| `ai-lab` | Python | 100 | ±0 | 4 |
| `claude-code-dev/projects/PJT001-stock-investment` | Python | 100 | ±0 | 1 |
| `soccer-manager` | Flutter | 95 | ±0 | 3 |
| `claude-code-dev` | monorepo | 79 | ±0 | 1 |
| `ai-side-business` | Python | 68 | ±0 | 6 |
| `tool-factory` | Python | 60 | ±0 | 7 |
| `kabu-agari-ranking` | Python | 55 | -45 | 10 |
| `price-tracker` | Python | 55 | ±0 | 6 |
| `quality-gainer-tracker` | Python | 28 | ±0 | 9 |
| `ir-analysis` | Python | 28 | ±0 | 6 |
| `youtube-video-creation` | 雛形のみ | 15 | ±0 | 2 |
| `claude-code-dev/projects/PJT002-ai-blog` | 雛形のみ | 15 | ±0 | 1 |
| `cohabitation-budget` | 雛形のみ | 0 | ±0 | 4 |
