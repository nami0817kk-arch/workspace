# 成長ループ ダイジェスト 2026-08-29

対象 7 プロジェクト / 平均成熟度 **72** / 未対応 6 件

## 今回の推奨アクション

### 1. [中] 依存バージョンが固定されていない

- 対象: `nami0817kk-arch/claude-code-dev/projects/PJT001-stock-investment`
- 種別: 動作の確からしさ / 4回目
- なぜ: `>=` だけだと、上流の新バージョンが出た日に、コードを何も変えていないのに壊れる。定期実行しているプロジェクトほど痛い。
- やること: 動いている今の環境で `pip freeze` を取り、`requirements.txt` を `==` で固定する。更新は Dependabot に任せて、上がったときに気づけるようにする。
参考: kabu-agari-ranking が既に同じことをやっているので、そこから写すのが早い。
- 根拠: `固定率 0% / 依存 12 件` / `お手本: nami0817kk-arch/kabu-agari-ranking`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/claude-code-dev/projects/PJT001-stock-investment の「依存バージョンが固定されていない」に対応してください。

背景: `>=` だけだと、上流の新バージョンが出た日に、コードを何も変えていないのに壊れる。定期実行しているプロジェクトほど痛い。
やること: 動いている今の環境で `pip freeze` を取り、`requirements.txt` を `==` で固定する。更新は Dependabot に任せて、上がったときに気づけるようにする。
参考: kabu-agari-ranking が既に同じことをやっているので、そこから写すのが早い。
お手本: kabu-agari-ranking の実装に合わせる。
該当: 固定率 0% / 依存 12 件

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

やらない場合: `python -m growth dismiss f700c6d39036 --note "理由"`
</details>

### 2. [低] 1ファイルが大きくなりすぎている

- 対象: `nami0817kk-arch/claude-code-dev/projects/PJT001-stock-investment`
- 種別: コード品質 / 4回目
- なぜ: 変更のたびに全体を読み直すことになり、手を入れる心理的コストが上がる。
- やること: 責務ごとにモジュールを分ける。まず「他から呼ばれていない塊」を別ファイルに出すのが安全。
- 根拠: `src/selector.py (675行)`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/claude-code-dev/projects/PJT001-stock-investment の「1ファイルが大きくなりすぎている」に対応してください。

背景: 変更のたびに全体を読み直すことになり、手を入れる心理的コストが上がる。
やること: 責務ごとにモジュールを分ける。まず「他から呼ばれていない塊」を別ファイルに出すのが安全。
該当: src/selector.py (675行)

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

やらない場合: `python -m growth dismiss 2737de7731d9 --note "理由"`
</details>

### 3. [低] 雛形だけ作られて中身が無い

- 対象: `nami0817kk-arch/claude-code-dev/projects/PJT002-ai-blog`
- 種別: コード品質 / 4回目
- なぜ: 空のプロジェクトが並んでいると、どれが生きているのか分からなくなり、点検のノイズにもなる。
- やること: 次の一手を1つだけ決めて README に書く（例:「最初に作る機能はこれ」）。当面やらないなら README にその旨を書くか、リポジトリをアーカイブする。

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/claude-code-dev/projects/PJT002-ai-blog の「雛形だけ作られて中身が無い」に対応してください。

背景: 空のプロジェクトが並んでいると、どれが生きているのか分からなくなり、点検のノイズにもなる。
やること: 次の一手を1つだけ決めて README に書く（例:「最初に作る機能はこれ」）。当面やらないなら README にその旨を書くか、リポジトリをアーカイブする。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

やらない場合: `python -m growth dismiss 0f8316fe910d --note "理由"`
</details>

### 4. [低] 1ファイルが大きくなりすぎている

- 対象: `nami0817kk-arch/claude-code-dev/projects/PJT003-soccer-manager`
- 種別: コード品質 / 4回目
- なぜ: 変更のたびに全体を読み直すことになり、手を入れる心理的コストが上がる。
- やること: 責務ごとにモジュールを分ける。まず「他から呼ばれていない塊」を別ファイルに出すのが安全。
- 根拠: `lib/logic/match_engine.dart (2299行)` / `lib/logic/player_generator.dart (412行)` / `lib/logic/training_engine.dart (700行)` / `lib/models/player.dart (1478行)` / `lib/models/save_game.dart (485行)` / `lib/screens/cup_screen.dart (552行)`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/claude-code-dev/projects/PJT003-soccer-manager の「1ファイルが大きくなりすぎている」に対応してください。

背景: 変更のたびに全体を読み直すことになり、手を入れる心理的コストが上がる。
やること: 責務ごとにモジュールを分ける。まず「他から呼ばれていない塊」を別ファイルに出すのが安全。
該当: lib/logic/match_engine.dart (2299行) / lib/logic/player_generator.dart (412行) / lib/logic/training_engine.dart (700行) / lib/models/player.dart (1478行) / lib/models/save_game.dart (485行)

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

やらない場合: `python -m growth dismiss 7776cdbdb0b1 --note "理由"`
</details>

### 5. [低] 雛形だけ作られて中身が無い

- 対象: `nami0817kk-arch/youtube-video-creation`
- 種別: コード品質 / 4回目
- なぜ: 空のプロジェクトが並んでいると、どれが生きているのか分からなくなり、点検のノイズにもなる。
- やること: 次の一手を1つだけ決めて README に書く（例:「最初に作る機能はこれ」）。当面やらないなら README にその旨を書くか、リポジトリをアーカイブする。

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/youtube-video-creation の「雛形だけ作られて中身が無い」に対応してください。

背景: 空のプロジェクトが並んでいると、どれが生きているのか分からなくなり、点検のノイズにもなる。
やること: 次の一手を1つだけ決めて README に書く（例:「最初に作る機能はこれ」）。当面やらないなら README にその旨を書くか、リポジトリをアーカイブする。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

やらない場合: `python -m growth dismiss 29142da01de3 --note "理由"`
</details>

### 6. [低] tests/ が空のまま置かれている

- 対象: `nami0817kk-arch/youtube-video-creation`
- 種別: 動作の確からしさ / 4回目
- なぜ: 雛形だけ作って中身が入っていない状態。あるように見えて何も守っていない。
- やること: 最初の1本を書くか、当面書かないなら tests/ を消して README に方針を書く。「あとで」を残しておくと、テストが無いことに気づけなくなる。

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/youtube-video-creation の「tests/ が空のまま置かれている」に対応してください。

背景: 雛形だけ作って中身が入っていない状態。あるように見えて何も守っていない。
やること: 最初の1本を書くか、当面書かないなら tests/ を消して README に方針を書く。「あとで」を残しておくと、テストが無いことに気づけなくなる。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

やらない場合: `python -m growth dismiss bef1c41fbf41 --note "理由"`
</details>

## プロジェクト別の成熟度

| プロジェクト | 種別 | 成熟度 | 前回比 | 未対応 |
|---|---|---:|---:|---:|
| `ai-lab` | Python | 100 | ±0 | 0 |
| `kabu-agari-ranking` | Python | 100 | +45 | 0 |
| `claude-code-dev/projects/PJT001-stock-investment` | Python | 100 | +52 | 2 |
| `claude-code-dev/projects/PJT003-soccer-manager` | Flutter | 95 | +7 | 1 |
| `claude-code-dev` | monorepo | 79 | +22 | 0 |
| `youtube-video-creation` | 雛形のみ | 15 | ±0 | 2 |
| `claude-code-dev/projects/PJT002-ai-blog` | 雛形のみ | 15 | ±0 | 1 |
