# 成長ループ ダイジェスト 2026-08-29

対象 7 プロジェクト / 平均成熟度 **54** / 未対応 23 件

## 今回の推奨アクション

### 1. [高] 定期実行ジョブが黙って失敗する状態になっている

- 対象: `nami0817kk-arch/kabu-agari-ranking`
- 種別: 自動化
- なぜ: スケジュール実行は、失敗しても手元で気づく機会がない。データ更新が止まっていることに何日も後で気づく、という壊れ方をする。
- やること: ワークフロー末尾に `if: failure()` の通知ステップを足す（Issue 起票 / メール / Webhook のいずれか）。GitHub は定期ジョブ失敗を通知しない設定のこともあるので、明示的に鳴らす。
- 根拠: `.github/workflows/daily-gainers-site.yml`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/kabu-agari-ranking の「定期実行ジョブが黙って失敗する状態になっている」に対応してください。

背景: スケジュール実行は、失敗しても手元で気づく機会がない。データ更新が止まっていることに何日も後で気づく、という壊れ方をする。
やること: ワークフロー末尾に `if: failure()` の通知ステップを足す（Issue 起票 / メール / Webhook のいずれか）。GitHub は定期ジョブ失敗を通知しない設定のこともあるので、明示的に鳴らす。
該当: .github/workflows/daily-gainers-site.yml

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

やらない場合: `python -m growth dismiss e7926b63dce1 --note "理由"`
</details>

### 2. [高] 環境変数を使っているのに .gitignore が .env を除外していない

- 対象: `nami0817kk-arch/kabu-agari-ranking`
- 種別: セキュリティ
- なぜ: ローカルで `.env` を作った瞬間に、うっかりコミットする事故が起きる。
- やること: `.gitignore` に `.env` を追加する。1行で終わる割に事故の期待値が大きい。

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/kabu-agari-ranking の「環境変数を使っているのに .gitignore が .env を除外していない」に対応してください。

背景: ローカルで `.env` を作った瞬間に、うっかりコミットする事故が起きる。
やること: `.gitignore` に `.env` を追加する。1行で終わる割に事故の期待値が大きい。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

やらない場合: `python -m growth dismiss f9fed0c9fb2b --note "理由"`
</details>

### 3. [高] 自動テストが1本もない

- 対象: `nami0817kk-arch/kabu-agari-ranking`
- 種別: 動作の確からしさ
- なぜ: コードが 4 ファイル / 約 537 行ある。この規模だと手で全部確かめるのは無理で、壊れたことに気づくのが本番になる。
- やること: まず一番壊れて困る関数1つに対してテストを1本書く。網羅は狙わない。`tests/test_<対象>.py` を作り、正常系1件・異常系1件から始める。
参考: claude-code-dev/projects/PJT003-soccer-manager が既に同じことをやっているので、そこから写すのが早い。
- 根拠: `お手本: nami0817kk-arch/claude-code-dev/projects/PJT003-soccer-manager`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/kabu-agari-ranking の「自動テストが1本もない」に対応してください。

背景: コードが 4 ファイル / 約 537 行ある。この規模だと手で全部確かめるのは無理で、壊れたことに気づくのが本番になる。
やること: まず一番壊れて困る関数1つに対してテストを1本書く。網羅は狙わない。`tests/test_<対象>.py` を作り、正常系1件・異常系1件から始める。
参考: claude-code-dev/projects/PJT003-soccer-manager が既に同じことをやっているので、そこから写すのが早い。
お手本: claude-code-dev/projects/PJT003-soccer-manager の実装に合わせる。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

やらない場合: `python -m growth dismiss 1e6f4f6cbdca --note "理由"`
</details>

### 4. [高] README が無い

- 対象: `nami0817kk-arch/claude-code-dev`
- 種別: ドキュメント
- なぜ: 何のプロジェクトで、どう動かすのかが分からない。数ヶ月後の自分が一番困る。
- やること: README.md に「目的」「動かし方」「構成」の3節を書く。長さは要らない。

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/claude-code-dev の「README が無い」に対応してください。

背景: 何のプロジェクトで、どう動かすのかが分からない。数ヶ月後の自分が一番困る。
やること: README.md に「目的」「動かし方」「構成」の3節を書く。長さは要らない。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

やらない場合: `python -m growth dismiss 0e36a3a6a2f6 --note "理由"`
</details>

### 5. [高] 自動テストが1本もない

- 対象: `nami0817kk-arch/claude-code-dev/projects/PJT001-stock-investment`
- 種別: 動作の確からしさ
- なぜ: コードが 14 ファイル / 約 2150 行ある。この規模だと手で全部確かめるのは無理で、壊れたことに気づくのが本番になる。
- やること: まず一番壊れて困る関数1つに対してテストを1本書く。網羅は狙わない。`tests/test_<対象>.py` を作り、正常系1件・異常系1件から始める。
参考: claude-code-dev/projects/PJT003-soccer-manager が既に同じことをやっているので、そこから写すのが早い。
- 根拠: `お手本: nami0817kk-arch/claude-code-dev/projects/PJT003-soccer-manager`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/claude-code-dev/projects/PJT001-stock-investment の「自動テストが1本もない」に対応してください。

背景: コードが 14 ファイル / 約 2150 行ある。この規模だと手で全部確かめるのは無理で、壊れたことに気づくのが本番になる。
やること: まず一番壊れて困る関数1つに対してテストを1本書く。網羅は狙わない。`tests/test_<対象>.py` を作り、正常系1件・異常系1件から始める。
参考: claude-code-dev/projects/PJT003-soccer-manager が既に同じことをやっているので、そこから写すのが早い。
お手本: claude-code-dev/projects/PJT003-soccer-manager の実装に合わせる。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

やらない場合: `python -m growth dismiss 56f3fe28fcdd --note "理由"`
</details>

### 6. [中] CI が無く、push しても誰も検証していない

- 対象: `nami0817kk-arch/claude-code-dev/projects/PJT001-stock-investment`
- 種別: 自動化
- なぜ: 壊れたコードが master に入っても気づけない。
- やること: `.github/workflows/ci.yml` を追加し、push と pull_request で依存インストール + テスト（または最低限 import チェック）を走らせる。
参考: claude-code-dev/projects/PJT003-soccer-manager が既に同じことをやっているので、そこから写すのが早い。
- 根拠: `お手本: nami0817kk-arch/claude-code-dev/projects/PJT003-soccer-manager`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/claude-code-dev/projects/PJT001-stock-investment の「CI が無く、push しても誰も検証していない」に対応してください。

背景: 壊れたコードが master に入っても気づけない。
やること: `.github/workflows/ci.yml` を追加し、push と pull_request で依存インストール + テスト（または最低限 import チェック）を走らせる。
参考: claude-code-dev/projects/PJT003-soccer-manager が既に同じことをやっているので、そこから写すのが早い。
お手本: claude-code-dev/projects/PJT003-soccer-manager の実装に合わせる。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

やらない場合: `python -m growth dismiss 50a0ad54de8a --note "理由"`
</details>

### 7. [中] 依存バージョンが固定されていない

- 対象: `nami0817kk-arch/claude-code-dev/projects/PJT001-stock-investment`
- 種別: 動作の確からしさ
- なぜ: `>=` だけだと、上流の新バージョンが出た日に、コードを何も変えていないのに壊れる。定期実行しているプロジェクトほど痛い。
- やること: 動いている今の環境で `pip freeze` を取り、`requirements.txt` を `==` で固定する。更新は Dependabot に任せて、上がったときに気づけるようにする。
- 根拠: `固定率 0% / 依存 12 件`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/claude-code-dev/projects/PJT001-stock-investment の「依存バージョンが固定されていない」に対応してください。

背景: `>=` だけだと、上流の新バージョンが出た日に、コードを何も変えていないのに壊れる。定期実行しているプロジェクトほど痛い。
やること: 動いている今の環境で `pip freeze` を取り、`requirements.txt` を `==` で固定する。更新は Dependabot に任せて、上がったときに気づけるようにする。
該当: 固定率 0% / 依存 12 件

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

やらない場合: `python -m growth dismiss f700c6d39036 --note "理由"`
</details>

### 8. [中] CLAUDE.md で AI に前提を渡す（PJT008 AIラボ / 成長ループ本体 で既に実践中）

- 対象: `nami0817kk-arch/claude-code-dev/projects/PJT002-ai-blog`
- 種別: 他PJTからの横展開
- なぜ: CLAUDE.md があると、毎回の依頼で環境や規約を説明し直さなくて済む。依頼のたびに前提を書く手間がそのまま消える。
- やること: お手本の CLAUDE.md を写し、このプロジェクト固有の「目的 / 構成 / 動かし方 / 気をつけること」に書き換える。
- 根拠: `お手本: nami0817kk-arch/ai-lab`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/claude-code-dev/projects/PJT002-ai-blog の「CLAUDE.md で AI に前提を渡す（PJT008 AIラボ / 成長ループ本体 で既に実践中）」に対応してください。

背景: CLAUDE.md があると、毎回の依頼で環境や規約を説明し直さなくて済む。依頼のたびに前提を書く手間がそのまま消える。
やること: お手本の CLAUDE.md を写し、このプロジェクト固有の「目的 / 構成 / 動かし方 / 気をつけること」に書き換える。
お手本: ai-lab の実装に合わせる。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

やらない場合: `python -m growth dismiss 9d52f669969d --note "理由"`
</details>

### 9. [中] CLAUDE.md で AI に前提を渡す（PJT008 AIラボ / 成長ループ本体 で既に実践中）

- 対象: `nami0817kk-arch/claude-code-dev/projects/PJT003-soccer-manager`
- 種別: 他PJTからの横展開
- なぜ: CLAUDE.md があると、毎回の依頼で環境や規約を説明し直さなくて済む。依頼のたびに前提を書く手間がそのまま消える。
- やること: お手本の CLAUDE.md を写し、このプロジェクト固有の「目的 / 構成 / 動かし方 / 気をつけること」に書き換える。
- 根拠: `お手本: nami0817kk-arch/ai-lab`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/claude-code-dev/projects/PJT003-soccer-manager の「CLAUDE.md で AI に前提を渡す（PJT008 AIラボ / 成長ループ本体 で既に実践中）」に対応してください。

背景: CLAUDE.md があると、毎回の依頼で環境や規約を説明し直さなくて済む。依頼のたびに前提を書く手間がそのまま消える。
やること: お手本の CLAUDE.md を写し、このプロジェクト固有の「目的 / 構成 / 動かし方 / 気をつけること」に書き換える。
お手本: ai-lab の実装に合わせる。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

やらない場合: `python -m growth dismiss c86d1b0d78c9 --note "理由"`
</details>

### 10. [中] CLAUDE.md で AI に前提を渡す（PJT008 AIラボ / 成長ループ本体 で既に実践中）

- 対象: `nami0817kk-arch/youtube-video-creation`
- 種別: 他PJTからの横展開
- なぜ: CLAUDE.md があると、毎回の依頼で環境や規約を説明し直さなくて済む。依頼のたびに前提を書く手間がそのまま消える。
- やること: お手本の CLAUDE.md を写し、このプロジェクト固有の「目的 / 構成 / 動かし方 / 気をつけること」に書き換える。
- 根拠: `お手本: nami0817kk-arch/ai-lab`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/youtube-video-creation の「CLAUDE.md で AI に前提を渡す（PJT008 AIラボ / 成長ループ本体 で既に実践中）」に対応してください。

背景: CLAUDE.md があると、毎回の依頼で環境や規約を説明し直さなくて済む。依頼のたびに前提を書く手間がそのまま消える。
やること: お手本の CLAUDE.md を写し、このプロジェクト固有の「目的 / 構成 / 動かし方 / 気をつけること」に書き換える。
お手本: ai-lab の実装に合わせる。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

やらない場合: `python -m growth dismiss 0234c880ac56 --note "理由"`
</details>

### 11. [低] 1ファイルが大きくなりすぎている

- 対象: `nami0817kk-arch/ai-lab`
- 種別: コード品質
- なぜ: 変更のたびに全体を読み直すことになり、手を入れる心理的コストが上がる。
- やること: 責務ごとにモジュールを分ける。まず「他から呼ばれていない塊」を別ファイルに出すのが安全。
- 根拠: `src/growth/rules.py (446行)`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/ai-lab の「1ファイルが大きくなりすぎている」に対応してください。

背景: 変更のたびに全体を読み直すことになり、手を入れる心理的コストが上がる。
やること: 責務ごとにモジュールを分ける。まず「他から呼ばれていない塊」を別ファイルに出すのが安全。
該当: src/growth/rules.py (446行)

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

やらない場合: `python -m growth dismiss a2e139dbddaf --note "理由"`
</details>

### 12. [低] 雛形だけ作られて中身が無い

- 対象: `nami0817kk-arch/claude-code-dev/projects/PJT002-ai-blog`
- 種別: コード品質
- なぜ: 空のプロジェクトが並んでいると、どれが生きているのか分からなくなり、点検のノイズにもなる。
- やること: 次の一手を1つだけ決めて README に書く（例:「最初に作る機能はこれ」）。当面やらないなら README にその旨を書くか、リポジトリをアーカイブする。

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/claude-code-dev/projects/PJT002-ai-blog の「雛形だけ作られて中身が無い」に対応してください。

背景: 空のプロジェクトが並んでいると、どれが生きているのか分からなくなり、点検のノイズにもなる。
やること: 次の一手を1つだけ決めて README に書く（例:「最初に作る機能はこれ」）。当面やらないなら README にその旨を書くか、リポジトリをアーカイブする。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

やらない場合: `python -m growth dismiss 0f8316fe910d --note "理由"`
</details>

## 見送った分

1回あたりの提案数の上限に達したため、11 件は次回以降に回した。

- `kabu-agari-ranking` 依存バージョンが固定されていない（中）
- `kabu-agari-ranking` 必要な環境変数の一覧が無い（中）
- `kabu-agari-ranking` CLAUDE.md で AI に前提を渡す（PJT008 AIラボ / 成長ループ本体 で既に実践中）（中）
- `claude-code-dev/projects/PJT001-stock-investment` CLAUDE.md で AI に前提を渡す（PJT008 AIラボ / 成長ループ本体 で既に実践中）（中）
- `kabu-agari-ranking` 依存更新の自動チェックが無い（低）
- `claude-code-dev/projects/PJT001-stock-investment` 依存更新の自動チェックが無い（低）
- `claude-code-dev/projects/PJT001-stock-investment` 1ファイルが大きくなりすぎている（低）
- `claude-code-dev/projects/PJT003-soccer-manager` 依存更新の自動チェックが無い（低）
- `claude-code-dev/projects/PJT003-soccer-manager` 1ファイルが大きくなりすぎている（低）
- `youtube-video-creation` 雛形だけ作られて中身が無い（低）
- `youtube-video-creation` tests/ が空のまま置かれている（低）

## プロジェクト別の成熟度

| プロジェクト | 種別 | 成熟度 | 前回比 | 未対応 |
|---|---|---:|---:|---:|
| `ai-lab` | Python | 100 | ±0 | 1 |
| `claude-code-dev/projects/PJT003-soccer-manager` | Flutter | 88 | ±0 | 3 |
| `claude-code-dev` | monorepo | 57 | ±0 | 1 |
| `kabu-agari-ranking` | Python | 55 | ±0 | 7 |
| `claude-code-dev/projects/PJT001-stock-investment` | Python | 48 | ±0 | 6 |
| `youtube-video-creation` | 雛形のみ | 15 | ±0 | 3 |
| `claude-code-dev/projects/PJT002-ai-blog` | 雛形のみ | 15 | ±0 | 2 |
