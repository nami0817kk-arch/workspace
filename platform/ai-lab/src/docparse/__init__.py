"""docparse - PDF から本文と表を、出典（ページ番号）つきで抜き出す。

決算短信・有価証券報告書・官公庁の資料といった「一次情報の原文」を機械で読むための道具。
`imagegen feed --source edinet` で見つけた書類や、`browser control pdf` で残したページを
そのまま渡す想定。

**どのページから拾った値なのかを必ず持ち回る。** 出典を言えない数字は一次情報として使えない。
"""

__version__ = "0.1.0"
