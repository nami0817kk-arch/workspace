# 成長ループ ダイジェスト 2026-09-07

対象 14 プロジェクト / 平均成熟度 **93** / 未対応 8 件

## ✅ 前回から解決したもの

- `workspace/libs/kabutan` .env.example で必要な設定を明示する（開発モノレポ（全PJT） で既に実践中）
- `workspace/projects/quality-gainer-tracker` 例外を握りつぶしている箇所がある
- `workspace/projects/kabu-agari-ranking` ドキュメントが存在しないファイルを載せている
- `workspace/projects/tool-factory` ドキュメントが存在しないファイルを載せている
- `workspace/projects/tool-factory` .env.example で必要な設定を明示する（開発モノレポ（全PJT） で既に実践中）
- `workspace/projects/youtube-video-creation` 必要な環境変数の一覧が無い
- `workspace/projects/youtube-video-creation` 依存バージョンが固定されていない
- `workspace/projects/gemini-api` CLAUDE.md で AI に前提を渡す（soccer-manager で既に実践中）
- `workspace/projects/soccer-manager` ドキュメントが存在しないファイルを載せている
- `workspace/projects/quality-gainer-tracker` 1つの関数が長くなりすぎている

## 今回の推奨アクション

### 1. [中] 1つの関数が長くなりすぎている

- 対象: `nami0817kk-arch/workspace`
- 種別: コード品質 / 2回目
- なぜ: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
- やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
- 根拠: `platform/ai-lab/src/imagegen/cli.py:41 build_parser() 180行` / `platform/ai-lab/src/imagegen/compose.py:126 compose() 136行`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace の「1つの関数が長くなりすぎている」に対応してください。

背景: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
該当: platform/ai-lab/src/imagegen/cli.py:41 build_parser() 180行 / platform/ai-lab/src/imagegen/compose.py:126 compose() 136行

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 3cbecdaad989 -n "理由"`
今後も出さない: `python -m growth dismiss 3cbecdaad989 -n "理由"`
</details>

### 2. [中] 1つの関数が長くなりすぎている

- 対象: `nami0817kk-arch/workspace/projects/ai-side-business`
- 種別: コード品質 / 2回目
- なぜ: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
- やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
- 根拠: `main.py:674 build_parser() 321行` / `src/screen.py:243 flow_chart() 129行`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/ai-side-business の「1つの関数が長くなりすぎている」に対応してください。

背景: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
該当: main.py:674 build_parser() 321行 / src/screen.py:243 flow_chart() 129行

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 946d7987cac7 -n "理由"`
今後も出さない: `python -m growth dismiss 946d7987cac7 -n "理由"`
</details>

### 3. [中] 依存バージョンが固定されていない

- 対象: `nami0817kk-arch/workspace/projects/stock-investment`
- 種別: 動作の確からしさ / 2回目
- なぜ: `>=` だけだと、上流の新バージョンが出た日に、コードを何も変えていないのに壊れる。定期実行しているプロジェクトほど痛い。
- やること: 動いている今の環境で `pip freeze` を取り、`requirements.txt` を `==` で固定する。更新は Dependabot に任せて、上がったときに気づけるようにする。
参考: workspace/projects/youtube-video-creation が既に同じことをやっているので、そこから写すのが早い。
- 根拠: `固定率 7% / 依存 14 件` / `お手本: nami0817kk-arch/workspace/projects/youtube-video-creation`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/stock-investment の「依存バージョンが固定されていない」に対応してください。

背景: `>=` だけだと、上流の新バージョンが出た日に、コードを何も変えていないのに壊れる。定期実行しているプロジェクトほど痛い。
やること: 動いている今の環境で `pip freeze` を取り、`requirements.txt` を `==` で固定する。更新は Dependabot に任せて、上がったときに気づけるようにする。
参考: workspace/projects/youtube-video-creation が既に同じことをやっているので、そこから写すのが早い。
お手本: workspace/projects/youtube-video-creation の実装に合わせる。
該当: 固定率 7% / 依存 14 件

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 219683e87101 -n "理由"`
今後も出さない: `python -m growth dismiss 219683e87101 -n "理由"`
</details>

### 4. [中] 1つの関数が長くなりすぎている

- 対象: `nami0817kk-arch/workspace/projects/stock-investment`
- 種別: コード品質 / 2回目
- なぜ: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
- やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
- 根拠: `src/selector.py:361 pick_from_news() 139行` / `src/selector.py:507 pick_from_youtube() 130行`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/stock-investment の「1つの関数が長くなりすぎている」に対応してください。

背景: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
該当: src/selector.py:361 pick_from_news() 139行 / src/selector.py:507 pick_from_youtube() 130行

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 666635dc2785 -n "理由"`
今後も出さない: `python -m growth dismiss 666635dc2785 -n "理由"`
</details>

### 5. [中] 例外を握りつぶしている箇所がある

- 対象: `nami0817kk-arch/workspace/projects/stock-investment`
- 種別: 動作の確からしさ / 2回目
- なぜ: `except ...: pass` は、失敗しても呼び出し側が何も知らないまま先へ進む。定期実行のように人が見ていない場所だと、壊れていることに誰も気づけない。
- やること: 最低でも失敗内容を print / logging で残す。「失敗しても続行してよい」場所なら、なぜよいのかをコメントに書く。
- 根拠: `src/report/news.py:327` / `src/selector.py:127`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/stock-investment の「例外を握りつぶしている箇所がある」に対応してください。

背景: `except ...: pass` は、失敗しても呼び出し側が何も知らないまま先へ進む。定期実行のように人が見ていない場所だと、壊れていることに誰も気づけない。
やること: 最低でも失敗内容を print / logging で残す。「失敗しても続行してよい」場所なら、なぜよいのかをコメントに書く。
該当: src/report/news.py:327 / src/selector.py:127

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 0f7e2c7eeeb2 -n "理由"`
今後も出さない: `python -m growth dismiss 0f7e2c7eeeb2 -n "理由"`
</details>

### 6. [中] 1つの関数が長くなりすぎている

- 対象: `nami0817kk-arch/workspace/projects/youtube-video-creation`
- 種別: コード品質 / 2回目
- なぜ: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
- やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
- 根拠: `src/candidates.py:284 assign() 140行` / `src/cli.py:1198 _cmd_x() 117行` / `src/cli.py:1318 _cmd_fetch() 127行` / `src/cli.py:165 main() 235行` / `src/cli.py:1714 _cmd_gather() 119行` / `src/cli.py:1961 _cmd_pick() 105行`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/youtube-video-creation の「1つの関数が長くなりすぎている」に対応してください。

背景: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
該当: src/candidates.py:284 assign() 140行 / src/cli.py:1198 _cmd_x() 117行 / src/cli.py:1318 _cmd_fetch() 127行 / src/cli.py:165 main() 235行 / src/cli.py:1714 _cmd_gather() 119行

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze b3e2f8965caf -n "理由"`
今後も出さない: `python -m growth dismiss b3e2f8965caf -n "理由"`
</details>

### 7. [低] 1ファイルが大きくなりすぎている

- 対象: `nami0817kk-arch/workspace/projects/soccer-clicker`
- 種別: コード品質
- なぜ: 変更のたびに全体を読み直すことになり、手を入れる心理的コストが上がる。
- やること: 責務ごとにモジュールを分ける。まず「他から呼ばれていない塊」を別ファイルに出すのが安全。
- 根拠: `lib/ui/game_screen.dart (408行)`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/soccer-clicker の「1ファイルが大きくなりすぎている」に対応してください。

背景: 変更のたびに全体を読み直すことになり、手を入れる心理的コストが上がる。
やること: 責務ごとにモジュールを分ける。まず「他から呼ばれていない塊」を別ファイルに出すのが安全。
該当: lib/ui/game_screen.dart (408行)

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 281d71410223 -n "理由"`
今後も出さない: `python -m growth dismiss 281d71410223 -n "理由"`
</details>

### 8. [低] 1ファイルが大きくなりすぎている

- 対象: `nami0817kk-arch/workspace/projects/soccer-manager`
- 種別: コード品質 / 2回目
- なぜ: 変更のたびに全体を読み直すことになり、手を入れる心理的コストが上がる。
- やること: 責務ごとにモジュールを分ける。まず「他から呼ばれていない塊」を別ファイルに出すのが安全。
- 根拠: `lib/data/glossary_entries.dart (653行)` / `lib/data/guide_sections.dart (538行)` / `lib/data/name_pool.dart (402行)` / `lib/logic/match_engine.dart (2670行)` / `lib/logic/player_generator.dart (564行)` / `lib/logic/training_engine.dart (988行)`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/soccer-manager の「1ファイルが大きくなりすぎている」に対応してください。

背景: 変更のたびに全体を読み直すことになり、手を入れる心理的コストが上がる。
やること: 責務ごとにモジュールを分ける。まず「他から呼ばれていない塊」を別ファイルに出すのが安全。
該当: lib/data/glossary_entries.dart (653行) / lib/data/guide_sections.dart (538行) / lib/data/name_pool.dart (402行) / lib/logic/match_engine.dart (2670行) / lib/logic/player_generator.dart (564行)

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze dd26b02a6594 -n "理由"`
今後も出さない: `python -m growth dismiss dd26b02a6594 -n "理由"`
</details>

## 保留中（理由あり）

見たうえで今はやらないと判断したもの。状況が変われば `growth run` の次回で戻せる。

- `workspace/projects/cohabitation-budget` 雛形だけ作られて中身が無い — index.html 1枚で完結させることが価値の完成品。README に「完成・運用中／構成を分割しない」と明記済み

## プロジェクト別の成熟度

| プロジェクト | 種別 | 成熟度 | 前回比 | 未対応 |
|---|---|---:|---:|---:|
| `workspace` | monorepo | 100 | ±0 | 1 |
| `workspace/projects/ai-side-business` | Python | 100 | ±0 | 1 |
| `workspace/projects/gemini-api` | Python | 100 | ±0 | 0 |
| `workspace/projects/ir-analysis` | Python | 100 | ±0 | 0 |
| `workspace/projects/kabu-agari-ranking` | Python | 100 | ±0 | 0 |
| `workspace/projects/price-tracker` | Python | 100 | ±0 | 0 |
| `workspace/projects/quality-gainer-tracker` | Python | 100 | ±0 | 0 |
| `workspace/projects/stock-investment` | Python | 100 | ±0 | 3 |
| `workspace/projects/tool-factory` | Python | 100 | ±0 | 0 |
| `workspace/projects/youtube-video-creation` | Python | 100 | ±0 | 1 |
| `workspace/projects/soccer-clicker` | Flutter | 95 | ±0 | 1 |
| `workspace/projects/soccer-manager` | Flutter | 95 | ±0 | 1 |
| `workspace/libs/kabutan` | Python | 95 | ±0 | 0 |
| `workspace/projects/cohabitation-budget` | 雛形のみ | 15 | ±0 | 0 |
