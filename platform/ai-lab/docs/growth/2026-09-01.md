# 成長ループ ダイジェスト 2026-09-01

対象 14 プロジェクト / 平均成熟度 **86** / 未対応 38 件

## ✅ 前回から解決したもの

- `youtube-video-creation` CLAUDE.md で AI に前提を渡す（サッカークラブ経営シミュレーション（Flutter） で既に実践中）
- `claude-code-dev` 公開リポジトリにライセンスが無い
- `quality-gainer-tracker` 自動テストが1本もない
- `soccer-manager` ワークフローに実行時間の上限が無い
- `claude-code-dev/projects/PJT002-ai-blog` 雛形だけ作られて中身が無い
- `quality-gainer-tracker` CLAUDE.md で AI に前提を渡す（サッカークラブ経営シミュレーション（Flutter） で既に実践中）
- `quality-gainer-tracker` .env.example で必要な設定を明示する（ai-lab / 成長ループ本体 で既に実践中）
- `quality-gainer-tracker` 依存バージョンが固定されていない
- `cohabitation-budget` README が無い
- `tool-factory` ドキュメント内のリンク先が存在しない
- `kabu-agari-ranking` 自動テストが1本もない
- `ir-analysis` 自動テストが1本もない
- `price-tracker` 公開リポジトリにライセンスが無い
- `youtube-video-creation` 雛形だけ作られて中身が無い
- `kabu-agari-ranking` 公開リポジトリにライセンスが無い
- `cohabitation-budget` 雛形だけ作られて中身が無い
- `price-tracker` CI が無く、push しても誰も検証していない
- `quality-gainer-tracker` 1つの関数が長くなりすぎている
- `ir-analysis` 依存更新の自動チェックが無い
- `ai-side-business` 依存更新の自動チェックが無い
- `price-tracker` 環境変数を使っているのに .gitignore が .env を除外していない
- `tool-factory` CLAUDE.md で AI に前提を渡す（サッカークラブ経営シミュレーション（Flutter） で既に実践中）
- `kabu-agari-ranking` 依存更新の自動チェックが無い
- `ai-lab` 1つの関数が長くなりすぎている
- `tool-factory` 依存パッケージがどこにも書かれていない
- `soccer-manager` ドキュメントが存在しないファイルを載せている
- `ai-lab` ドキュメント内のリンク先が存在しない
- `soccer-manager` 1ファイルが大きくなりすぎている
- `price-tracker` CLAUDE.md で AI に前提を渡す（サッカークラブ経営シミュレーション（Flutter） で既に実践中）
- `ai-side-business` 1つの関数が長くなりすぎている
- `claude-code-dev/projects/PJT001-stock-investment` 例外を握りつぶしている箇所がある
- `kabu-agari-ranking` 必要な環境変数の一覧が無い
- `tool-factory` .env.example で必要な設定を明示する（ai-lab / 成長ループ本体 で既に実践中）
- `ai-side-business` 依存バージョンが固定されていない
- `kabu-agari-ranking` 例外を握りつぶしている箇所がある
- `ai-side-business` CI が無く、push しても誰も検証していない
- `quality-gainer-tracker` 依存更新の自動チェックが無い
- `ai-lab` ドキュメントが存在しないファイルを載せている
- `cohabitation-budget` CLAUDE.md で AI に前提を渡す（サッカークラブ経営シミュレーション（Flutter） で既に実践中）
- `cohabitation-budget` 公開リポジトリにライセンスが無い
- `kabu-agari-ranking` 依存バージョンが固定されていない
- `quality-gainer-tracker` CI が無く、push しても誰も検証していない
- `ir-analysis` CLAUDE.md で AI に前提を渡す（サッカークラブ経営シミュレーション（Flutter） で既に実践中）
- `tool-factory` CI が無く、push しても誰も検証していない
- `price-tracker` 必要な環境変数の一覧が無い
- `claude-code-dev/projects/PJT001-stock-investment` 1つの関数が長くなりすぎている
- `kabu-agari-ranking` CLAUDE.md で AI に前提を渡す（サッカークラブ経営シミュレーション（Flutter） で既に実践中）
- `youtube-video-creation` tests/ が空のまま置かれている
- `kabu-agari-ranking` ワークフローに実行時間の上限が無い
- `price-tracker` 依存パッケージがどこにも書かれていない
- `ir-analysis` 依存バージョンが固定されていない
- `ir-analysis` README が無い
- `tool-factory` ドキュメントが存在しないファイルを載せている
- `ai-lab` 依存バージョンが固定されていない
- `tool-factory` 公開リポジトリにライセンスが無い
- `ai-side-business` ドキュメントが存在しないファイルを載せている
- `ir-analysis` CI が無く、push しても誰も検証していない
- `kabu-agari-ranking` 定期実行ジョブが黙って失敗する状態になっている
- `ai-side-business` CLAUDE.md で AI に前提を渡す（サッカークラブ経営シミュレーション（Flutter） で既に実践中）
- `quality-gainer-tracker` 例外を握りつぶしている箇所がある
- `claude-code-dev/projects/PJT001-stock-investment` 依存バージョンが固定されていない
- `quality-gainer-tracker` README が無い
- `kabu-agari-ranking` 環境変数を使っているのに .gitignore が .env を除外していない

## 今回の推奨アクション

### 1. [高] README が無い

- 対象: `nami0817kk-arch/workspace`
- 種別: ドキュメント
- なぜ: 何のプロジェクトで、どう動かすのかが分からない。数ヶ月後の自分が一番困る。
- やること: README.md に「目的」「動かし方」「構成」の3節を書く。長さは要らない。

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace の「README が無い」に対応してください。

背景: 何のプロジェクトで、どう動かすのかが分からない。数ヶ月後の自分が一番困る。
やること: README.md に「目的」「動かし方」「構成」の3節を書く。長さは要らない。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 3e08ad62e194 -n "理由"`
今後も出さない: `python -m growth dismiss 3e08ad62e194 -n "理由"`
</details>

### 2. [高] 自動テストが1本もない

- 対象: `nami0817kk-arch/workspace/projects/ai-blog`
- 種別: 動作の確からしさ
- なぜ: コードが 2 ファイル / 約 91 行ある。この規模だと手で全部確かめるのは無理で、壊れたことに気づくのが本番になる。
- やること: まず一番壊れて困る関数1つに対してテストを1本書く。網羅は狙わない。`tests/test_<対象>.py` を作り、正常系1件・異常系1件から始める。
参考: workspace/projects/soccer-manager が既に同じことをやっているので、そこから写すのが早い。
- 根拠: `お手本: nami0817kk-arch/workspace/projects/soccer-manager`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/ai-blog の「自動テストが1本もない」に対応してください。

背景: コードが 2 ファイル / 約 91 行ある。この規模だと手で全部確かめるのは無理で、壊れたことに気づくのが本番になる。
やること: まず一番壊れて困る関数1つに対してテストを1本書く。網羅は狙わない。`tests/test_<対象>.py` を作り、正常系1件・異常系1件から始める。
参考: workspace/projects/soccer-manager が既に同じことをやっているので、そこから写すのが早い。
お手本: workspace/projects/soccer-manager の実装に合わせる。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 72e7d5f4fb4b -n "理由"`
今後も出さない: `python -m growth dismiss 72e7d5f4fb4b -n "理由"`
</details>

### 3. [中] ワークフローに実行時間の上限が無い

- 対象: `nami0817kk-arch/workspace`
- 種別: 自動化
- なぜ: ネットワーク待ちなどでジョブがハングすると、GitHub の既定では6時間走り続けて Actions の実行時間を食い潰す。しかも失敗として扱われるのは6時間後になる。
- やること: 各 job に `timeout-minutes:` を入れる（テストなら10分、取得や公開を伴うものでも30分あれば足りる）。
- 根拠: `.github/workflows/ai-side-business-tests.yml` / `.github/workflows/gemini-api-tests.yml` / `.github/workflows/ir-analysis-tests.yml` / `.github/workflows/kabu-tests.yml` / `.github/workflows/kabutan-lib-tests.yml` / `.github/workflows/price-tracker-tests.yml`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace の「ワークフローに実行時間の上限が無い」に対応してください。

背景: ネットワーク待ちなどでジョブがハングすると、GitHub の既定では6時間走り続けて Actions の実行時間を食い潰す。しかも失敗として扱われるのは6時間後になる。
やること: 各 job に `timeout-minutes:` を入れる（テストなら10分、取得や公開を伴うものでも30分あれば足りる）。
該当: .github/workflows/ai-side-business-tests.yml / .github/workflows/gemini-api-tests.yml / .github/workflows/ir-analysis-tests.yml / .github/workflows/kabu-tests.yml / .github/workflows/kabutan-lib-tests.yml

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 026a9c4b42b7 -n "理由"`
今後も出さない: `python -m growth dismiss 026a9c4b42b7 -n "理由"`
</details>

### 4. [中] 1つの関数が長くなりすぎている

- 対象: `nami0817kk-arch/workspace`
- 種別: コード品質
- なぜ: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
- やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
- 根拠: `platform/ai-lab/src/imagegen/cli.py:41 build_parser() 180行` / `platform/ai-lab/src/imagegen/compose.py:126 compose() 136行` / `platform/ai-lab/src/imagegen/mcp_server.py:41 tool_definitions() 198行`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace の「1つの関数が長くなりすぎている」に対応してください。

背景: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
該当: platform/ai-lab/src/imagegen/cli.py:41 build_parser() 180行 / platform/ai-lab/src/imagegen/compose.py:126 compose() 136行 / platform/ai-lab/src/imagegen/mcp_server.py:41 tool_definitions() 198行

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 3cbecdaad989 -n "理由"`
今後も出さない: `python -m growth dismiss 3cbecdaad989 -n "理由"`
</details>

### 5. [中] ワークフローに実行時間の上限が無い

- 対象: `nami0817kk-arch/workspace/libs/kabutan`
- 種別: 自動化
- なぜ: ネットワーク待ちなどでジョブがハングすると、GitHub の既定では6時間走り続けて Actions の実行時間を食い潰す。しかも失敗として扱われるのは6時間後になる。
- やること: 各 job に `timeout-minutes:` を入れる（テストなら10分、取得や公開を伴うものでも30分あれば足りる）。
- 根拠: `.github/workflows/kabu-tests.yml` / `.github/workflows/kabutan-lib-tests.yml` / `.github/workflows/quality-gainer-tracker-tests.yml`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/libs/kabutan の「ワークフローに実行時間の上限が無い」に対応してください。

背景: ネットワーク待ちなどでジョブがハングすると、GitHub の既定では6時間走り続けて Actions の実行時間を食い潰す。しかも失敗として扱われるのは6時間後になる。
やること: 各 job に `timeout-minutes:` を入れる（テストなら10分、取得や公開を伴うものでも30分あれば足りる）。
該当: .github/workflows/kabu-tests.yml / .github/workflows/kabutan-lib-tests.yml / .github/workflows/quality-gainer-tracker-tests.yml

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 516eb2e25755 -n "理由"`
今後も出さない: `python -m growth dismiss 516eb2e25755 -n "理由"`
</details>

### 6. [中] CI が無く、push しても誰も検証していない

- 対象: `nami0817kk-arch/workspace/projects/ai-blog`
- 種別: 自動化
- なぜ: 壊れたコードが master に入っても気づけない。
- やること: `.github/workflows/ci.yml` を追加し、push と pull_request で依存インストール + テスト（または最低限 import チェック）を走らせる。
参考: workspace/projects/soccer-manager が既に同じことをやっているので、そこから写すのが早い。
- 根拠: `お手本: nami0817kk-arch/workspace/projects/soccer-manager`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/ai-blog の「CI が無く、push しても誰も検証していない」に対応してください。

背景: 壊れたコードが master に入っても気づけない。
やること: `.github/workflows/ci.yml` を追加し、push と pull_request で依存インストール + テスト（または最低限 import チェック）を走らせる。
参考: workspace/projects/soccer-manager が既に同じことをやっているので、そこから写すのが早い。
お手本: workspace/projects/soccer-manager の実装に合わせる。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze f1106e728c4f -n "理由"`
今後も出さない: `python -m growth dismiss f1106e728c4f -n "理由"`
</details>

### 7. [中] 依存バージョンが固定されていない

- 対象: `nami0817kk-arch/workspace/projects/ai-blog`
- 種別: 動作の確からしさ
- なぜ: `>=` だけだと、上流の新バージョンが出た日に、コードを何も変えていないのに壊れる。定期実行しているプロジェクトほど痛い。
- やること: 動いている今の環境で `pip freeze` を取り、`requirements.txt` を `==` で固定する。更新は Dependabot に任せて、上がったときに気づけるようにする。
参考: workspace/projects/ai-side-business が既に同じことをやっているので、そこから写すのが早い。
- 根拠: `固定率 0% / 依存 2 件` / `お手本: nami0817kk-arch/workspace/projects/ai-side-business`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/ai-blog の「依存バージョンが固定されていない」に対応してください。

背景: `>=` だけだと、上流の新バージョンが出た日に、コードを何も変えていないのに壊れる。定期実行しているプロジェクトほど痛い。
やること: 動いている今の環境で `pip freeze` を取り、`requirements.txt` を `==` で固定する。更新は Dependabot に任せて、上がったときに気づけるようにする。
参考: workspace/projects/ai-side-business が既に同じことをやっているので、そこから写すのが早い。
お手本: workspace/projects/ai-side-business の実装に合わせる。
該当: 固定率 0% / 依存 2 件

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze c438bc0959a7 -n "理由"`
今後も出さない: `python -m growth dismiss c438bc0959a7 -n "理由"`
</details>

### 8. [中] ワークフローに実行時間の上限が無い

- 対象: `nami0817kk-arch/workspace/projects/ai-side-business`
- 種別: 自動化
- なぜ: ネットワーク待ちなどでジョブがハングすると、GitHub の既定では6時間走り続けて Actions の実行時間を食い潰す。しかも失敗として扱われるのは6時間後になる。
- やること: 各 job に `timeout-minutes:` を入れる（テストなら10分、取得や公開を伴うものでも30分あれば足りる）。
- 根拠: `.github/workflows/ai-side-business-tests.yml`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/ai-side-business の「ワークフローに実行時間の上限が無い」に対応してください。

背景: ネットワーク待ちなどでジョブがハングすると、GitHub の既定では6時間走り続けて Actions の実行時間を食い潰す。しかも失敗として扱われるのは6時間後になる。
やること: 各 job に `timeout-minutes:` を入れる（テストなら10分、取得や公開を伴うものでも30分あれば足りる）。
該当: .github/workflows/ai-side-business-tests.yml

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 872145fabf41 -n "理由"`
今後も出さない: `python -m growth dismiss 872145fabf41 -n "理由"`
</details>

### 9. [中] 1つの関数が長くなりすぎている

- 対象: `nami0817kk-arch/workspace/projects/ai-side-business`
- 種別: コード品質
- なぜ: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
- やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
- 根拠: `main.py:426 cmd_media() 115行` / `main.py:650 build_parser() 321行` / `src/screen.py:243 flow_chart() 129行`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/ai-side-business の「1つの関数が長くなりすぎている」に対応してください。

背景: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
該当: main.py:426 cmd_media() 115行 / main.py:650 build_parser() 321行 / src/screen.py:243 flow_chart() 129行

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 946d7987cac7 -n "理由"`
今後も出さない: `python -m growth dismiss 946d7987cac7 -n "理由"`
</details>

### 10. [中] ワークフローに実行時間の上限が無い

- 対象: `nami0817kk-arch/workspace/projects/gemini-api`
- 種別: 自動化
- なぜ: ネットワーク待ちなどでジョブがハングすると、GitHub の既定では6時間走り続けて Actions の実行時間を食い潰す。しかも失敗として扱われるのは6時間後になる。
- やること: 各 job に `timeout-minutes:` を入れる（テストなら10分、取得や公開を伴うものでも30分あれば足りる）。
- 根拠: `.github/workflows/gemini-api-tests.yml`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/gemini-api の「ワークフローに実行時間の上限が無い」に対応してください。

背景: ネットワーク待ちなどでジョブがハングすると、GitHub の既定では6時間走り続けて Actions の実行時間を食い潰す。しかも失敗として扱われるのは6時間後になる。
やること: 各 job に `timeout-minutes:` を入れる（テストなら10分、取得や公開を伴うものでも30分あれば足りる）。
該当: .github/workflows/gemini-api-tests.yml

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze c8ade36d74fc -n "理由"`
今後も出さない: `python -m growth dismiss c8ade36d74fc -n "理由"`
</details>

### 11. [中] 依存バージョンが固定されていない

- 対象: `nami0817kk-arch/workspace/projects/gemini-api`
- 種別: 動作の確からしさ
- なぜ: `>=` だけだと、上流の新バージョンが出た日に、コードを何も変えていないのに壊れる。定期実行しているプロジェクトほど痛い。
- やること: 動いている今の環境で `pip freeze` を取り、`requirements.txt` を `==` で固定する。更新は Dependabot に任せて、上がったときに気づけるようにする。
参考: workspace/projects/ai-side-business が既に同じことをやっているので、そこから写すのが早い。
- 根拠: `固定率 0% / 依存 5 件` / `お手本: nami0817kk-arch/workspace/projects/ai-side-business`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/gemini-api の「依存バージョンが固定されていない」に対応してください。

背景: `>=` だけだと、上流の新バージョンが出た日に、コードを何も変えていないのに壊れる。定期実行しているプロジェクトほど痛い。
やること: 動いている今の環境で `pip freeze` を取り、`requirements.txt` を `==` で固定する。更新は Dependabot に任せて、上がったときに気づけるようにする。
参考: workspace/projects/ai-side-business が既に同じことをやっているので、そこから写すのが早い。
お手本: workspace/projects/ai-side-business の実装に合わせる。
該当: 固定率 0% / 依存 5 件

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 85d68187ef12 -n "理由"`
今後も出さない: `python -m growth dismiss 85d68187ef12 -n "理由"`
</details>

### 12. [中] 例外を握りつぶしている箇所がある

- 対象: `nami0817kk-arch/workspace/projects/gemini-api`
- 種別: 動作の確からしさ
- なぜ: `except ...: pass` は、失敗しても呼び出し側が何も知らないまま先へ進む。定期実行のように人が見ていない場所だと、壊れていることに誰も気づけない。
- やること: 最低でも失敗内容を print / logging で残す。「失敗しても続行してよい」場所なら、なぜよいのかをコメントに書く。
- 根拠: `app/services/cache.py:130`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/gemini-api の「例外を握りつぶしている箇所がある」に対応してください。

背景: `except ...: pass` は、失敗しても呼び出し側が何も知らないまま先へ進む。定期実行のように人が見ていない場所だと、壊れていることに誰も気づけない。
やること: 最低でも失敗内容を print / logging で残す。「失敗しても続行してよい」場所なら、なぜよいのかをコメントに書く。
該当: app/services/cache.py:130

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze b02bab0a7f0e -n "理由"`
今後も出さない: `python -m growth dismiss b02bab0a7f0e -n "理由"`
</details>

## あなたの判断が要るもの

これは作業ではなく決めごと。決まるまで私の側では動かせない。

- `workspace/projects/cohabitation-budget` **雛形だけ作られて中身が無い** — 次の一手を1つだけ決めて README に書く（例:「最初に作る機能はこれ」）。当面やらないなら README にその旨を書くか、リポジトリをアーカイブする。

## 見送った分

1回あたりの提案数の上限に達したため、26 件は次回以降に回した。

- `workspace/projects/ai-blog` README に「動かし方」のコマンドが書かれていない（中）
- `workspace/projects/gemini-api` CLAUDE.md で AI に前提を渡す（soccer-manager で既に実践中）（中）
- `workspace/projects/ir-analysis` ワークフローに実行時間の上限が無い（中）
- `workspace/projects/kabu-agari-ranking` ワークフローに実行時間の上限が無い（中）
- `workspace/projects/price-tracker` ワークフローに実行時間の上限が無い（中）
- `workspace/projects/quality-gainer-tracker` ワークフローに実行時間の上限が無い（中）
- `workspace/projects/quality-gainer-tracker` 1つの関数が長くなりすぎている（中）
- `workspace/projects/quality-gainer-tracker` 例外を握りつぶしている箇所がある（中）
- `workspace/projects/stock-investment` 依存バージョンが固定されていない（中）
- `workspace/projects/stock-investment` 1つの関数が長くなりすぎている（中）
- `workspace/projects/stock-investment` 例外を握りつぶしている箇所がある（中）
- `workspace/projects/youtube-video-creation` ワークフローに実行時間の上限が無い（中）
- `workspace/projects/youtube-video-creation` 依存バージョンが固定されていない（中）
- `workspace/projects/youtube-video-creation` 必要な環境変数の一覧が無い（中）
- `workspace/projects/youtube-video-creation` 1つの関数が長くなりすぎている（中）
- `workspace` ドキュメント内のリンク先が存在しない（低）
- `workspace` ドキュメントが存在しないファイルを載せている（低）
- `workspace/libs/kabutan` .env.example で必要な設定を明示する（開発モノレポ（全PJT） で既に実践中）（低）
- `workspace/projects/kabu-agari-ranking` ドキュメントが存在しないファイルを載せている（低）
- `workspace/projects/price-tracker` 依存バージョンを固定する（ai-side-business で既に実践中）（低）

## プロジェクト別の成熟度

| プロジェクト | 種別 | 成熟度 | 前回比 | 未対応 |
|---|---|---:|---:|---:|
| `workspace/projects/ai-side-business` | Python | 100 | ±0 | 2 |
| `workspace/projects/ir-analysis` | Python | 100 | ±0 | 1 |
| `workspace/projects/kabu-agari-ranking` | Python | 100 | ±0 | 2 |
| `workspace/projects/price-tracker` | Python | 100 | ±0 | 2 |
| `workspace/projects/quality-gainer-tracker` | Python | 100 | ±0 | 3 |
| `workspace/projects/stock-investment` | Python | 100 | ±0 | 3 |
| `workspace/projects/tool-factory` | Python | 100 | ±0 | 3 |
| `workspace/projects/youtube-video-creation` | Python | 100 | ±0 | 5 |
| `workspace/projects/soccer-manager` | Flutter | 95 | ±0 | 2 |
| `workspace/libs/kabutan` | Python | 95 | ±0 | 2 |
| `workspace/projects/gemini-api` | Python | 93 | ±0 | 4 |
| `workspace` | monorepo | 57 | ±0 | 5 |
| `workspace/projects/ai-blog` | Python | 45 | ±0 | 4 |
| `workspace/projects/cohabitation-budget` | 雛形のみ | 15 | ±0 | 1 |
