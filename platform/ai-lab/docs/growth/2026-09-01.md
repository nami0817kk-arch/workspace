# 成長ループ ダイジェスト 2026-09-01

対象 13 プロジェクト / 平均成熟度 **92** / 未対応 18 件

## ✅ 前回から解決したもの

- `workspace/projects/youtube-video-creation` ワークフローに実行時間の上限が無い
- `workspace/projects/quality-gainer-tracker` ワークフローに実行時間の上限が無い
- `workspace` README が無い
- `workspace/projects/price-tracker` ワークフローに実行時間の上限が無い
- `workspace/projects/ai-blog` README に「動かし方」のコマンドが書かれていない
- `workspace/projects/ai-blog` 自動テストが1本もない
- `workspace/projects/ir-analysis` ワークフローに実行時間の上限が無い
- `workspace/projects/gemini-api` 依存バージョンが固定されていない
- `workspace/projects/ai-side-business` ワークフローに実行時間の上限が無い
- `workspace/projects/gemini-api` 例外を握りつぶしている箇所がある
- `workspace/projects/ai-blog` 依存バージョンが固定されていない
- `workspace/projects/gemini-api` ワークフローに実行時間の上限が無い
- `workspace/projects/kabu-agari-ranking` ワークフローに実行時間の上限が無い
- `workspace/projects/ai-blog` CI が無く、push しても誰も検証していない

## 今回の推奨アクション

### 1. [中] 1つの関数が長くなりすぎている

- 対象: `nami0817kk-arch/workspace`
- 種別: コード品質
- なぜ: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
- やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
- 根拠: `platform/ai-lab/src/imagegen/cli.py:41 build_parser() 180行` / `platform/ai-lab/src/imagegen/compose.py:126 compose() 136行` / `platform/ai-lab/src/imagegen/mcp_server.py:41 tool_definitions() 198行`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace の「1つの関数が長くなりすぎている」に対応してください。

背景: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
該当: platform/ai-lab/src/imagegen/cli.py:41 build_parser() 180行 / platform/ai-lab/src/imagegen/compose.py:126 compose() 136行 / platform/ai-lab/src/imagegen/mcp_server.py:41 tool_definitions() 198行

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 3cbecdaad989 -n "理由"`
今後も出さない: `python -m growth dismiss 3cbecdaad989 -n "理由"`
</details>

### 2. [中] 1つの関数が長くなりすぎている

- 対象: `nami0817kk-arch/workspace/projects/ai-side-business`
- 種別: コード品質
- なぜ: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
- やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
- 根拠: `main.py:426 cmd_media() 115行` / `main.py:650 build_parser() 321行` / `src/screen.py:243 flow_chart() 129行`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/ai-side-business の「1つの関数が長くなりすぎている」に対応してください。

背景: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
該当: main.py:426 cmd_media() 115行 / main.py:650 build_parser() 321行 / src/screen.py:243 flow_chart() 129行

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 946d7987cac7 -n "理由"`
今後も出さない: `python -m growth dismiss 946d7987cac7 -n "理由"`
</details>

### 3. [中] CLAUDE.md で AI に前提を渡す（soccer-manager で既に実践中）

- 対象: `nami0817kk-arch/workspace/projects/gemini-api`
- 種別: 他PJTからの横展開
- なぜ: CLAUDE.md があると、毎回の依頼で環境や規約を説明し直さなくて済む。依頼のたびに前提を書く手間がそのまま消える。
- やること: お手本の CLAUDE.md を写し、このプロジェクト固有の「目的 / 構成 / 動かし方 / 気をつけること」に書き換える。
- 根拠: `お手本: nami0817kk-arch/workspace/projects/soccer-manager`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/gemini-api の「CLAUDE.md で AI に前提を渡す（soccer-manager で既に実践中）」に対応してください。

背景: CLAUDE.md があると、毎回の依頼で環境や規約を説明し直さなくて済む。依頼のたびに前提を書く手間がそのまま消える。
やること: お手本の CLAUDE.md を写し、このプロジェクト固有の「目的 / 構成 / 動かし方 / 気をつけること」に書き換える。
お手本: workspace/projects/soccer-manager の実装に合わせる。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze a1d6ca32d45a -n "理由"`
今後も出さない: `python -m growth dismiss a1d6ca32d45a -n "理由"`
</details>

### 4. [中] 1つの関数が長くなりすぎている

- 対象: `nami0817kk-arch/workspace/projects/quality-gainer-tracker`
- 種別: コード品質
- なぜ: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
- やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
- 根拠: `src/analysis/pattern_detector.py:64 detect_ab() 119行`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/quality-gainer-tracker の「1つの関数が長くなりすぎている」に対応してください。

背景: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
該当: src/analysis/pattern_detector.py:64 detect_ab() 119行

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze f5d43d1bee7a -n "理由"`
今後も出さない: `python -m growth dismiss f5d43d1bee7a -n "理由"`
</details>

### 5. [中] 例外を握りつぶしている箇所がある

- 対象: `nami0817kk-arch/workspace/projects/quality-gainer-tracker`
- 種別: 動作の確からしさ
- なぜ: `except ...: pass` は、失敗しても呼び出し側が何も知らないまま先へ進む。定期実行のように人が見ていない場所だと、壊れていることに誰も気づけない。
- やること: 最低でも失敗内容を print / logging で残す。「失敗しても続行してよい」場所なら、なぜよいのかをコメントに書く。
- 根拠: `src/db/manager.py:227`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/quality-gainer-tracker の「例外を握りつぶしている箇所がある」に対応してください。

背景: `except ...: pass` は、失敗しても呼び出し側が何も知らないまま先へ進む。定期実行のように人が見ていない場所だと、壊れていることに誰も気づけない。
やること: 最低でも失敗内容を print / logging で残す。「失敗しても続行してよい」場所なら、なぜよいのかをコメントに書く。
該当: src/db/manager.py:227

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 0e30629abc0f -n "理由"`
今後も出さない: `python -m growth dismiss 0e30629abc0f -n "理由"`
</details>

### 6. [中] 依存バージョンが固定されていない

- 対象: `nami0817kk-arch/workspace/projects/stock-investment`
- 種別: 動作の確からしさ
- なぜ: `>=` だけだと、上流の新バージョンが出た日に、コードを何も変えていないのに壊れる。定期実行しているプロジェクトほど痛い。
- やること: 動いている今の環境で `pip freeze` を取り、`requirements.txt` を `==` で固定する。更新は Dependabot に任せて、上がったときに気づけるようにする。
参考: workspace/projects/ai-side-business が既に同じことをやっているので、そこから写すのが早い。
- 根拠: `固定率 0% / 依存 12 件` / `お手本: nami0817kk-arch/workspace/projects/ai-side-business`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/stock-investment の「依存バージョンが固定されていない」に対応してください。

背景: `>=` だけだと、上流の新バージョンが出た日に、コードを何も変えていないのに壊れる。定期実行しているプロジェクトほど痛い。
やること: 動いている今の環境で `pip freeze` を取り、`requirements.txt` を `==` で固定する。更新は Dependabot に任せて、上がったときに気づけるようにする。
参考: workspace/projects/ai-side-business が既に同じことをやっているので、そこから写すのが早い。
お手本: workspace/projects/ai-side-business の実装に合わせる。
該当: 固定率 0% / 依存 12 件

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 219683e87101 -n "理由"`
今後も出さない: `python -m growth dismiss 219683e87101 -n "理由"`
</details>

### 7. [中] 1つの関数が長くなりすぎている

- 対象: `nami0817kk-arch/workspace/projects/stock-investment`
- 種別: コード品質
- なぜ: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
- やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
- 根拠: `src/selector.py:361 pick_from_news() 139行` / `src/selector.py:507 pick_from_youtube() 130行`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/stock-investment の「1つの関数が長くなりすぎている」に対応してください。

背景: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
該当: src/selector.py:361 pick_from_news() 139行 / src/selector.py:507 pick_from_youtube() 130行

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 666635dc2785 -n "理由"`
今後も出さない: `python -m growth dismiss 666635dc2785 -n "理由"`
</details>

### 8. [中] 例外を握りつぶしている箇所がある

- 対象: `nami0817kk-arch/workspace/projects/stock-investment`
- 種別: 動作の確からしさ
- なぜ: `except ...: pass` は、失敗しても呼び出し側が何も知らないまま先へ進む。定期実行のように人が見ていない場所だと、壊れていることに誰も気づけない。
- やること: 最低でも失敗内容を print / logging で残す。「失敗しても続行してよい」場所なら、なぜよいのかをコメントに書く。
- 根拠: `src/report/news.py:250` / `src/report/news.py:324` / `src/report/news.py:378` / `src/selector.py:127`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/stock-investment の「例外を握りつぶしている箇所がある」に対応してください。

背景: `except ...: pass` は、失敗しても呼び出し側が何も知らないまま先へ進む。定期実行のように人が見ていない場所だと、壊れていることに誰も気づけない。
やること: 最低でも失敗内容を print / logging で残す。「失敗しても続行してよい」場所なら、なぜよいのかをコメントに書く。
該当: src/report/news.py:250 / src/report/news.py:324 / src/report/news.py:378 / src/selector.py:127

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 0f7e2c7eeeb2 -n "理由"`
今後も出さない: `python -m growth dismiss 0f7e2c7eeeb2 -n "理由"`
</details>

### 9. [中] 依存バージョンが固定されていない

- 対象: `nami0817kk-arch/workspace/projects/youtube-video-creation`
- 種別: 動作の確からしさ
- なぜ: `>=` だけだと、上流の新バージョンが出た日に、コードを何も変えていないのに壊れる。定期実行しているプロジェクトほど痛い。
- やること: 動いている今の環境で `pip freeze` を取り、`requirements.txt` を `==` で固定する。更新は Dependabot に任せて、上がったときに気づけるようにする。
参考: workspace/projects/ai-side-business が既に同じことをやっているので、そこから写すのが早い。
- 根拠: `固定率 0% / 依存 5 件` / `お手本: nami0817kk-arch/workspace/projects/ai-side-business`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/youtube-video-creation の「依存バージョンが固定されていない」に対応してください。

背景: `>=` だけだと、上流の新バージョンが出た日に、コードを何も変えていないのに壊れる。定期実行しているプロジェクトほど痛い。
やること: 動いている今の環境で `pip freeze` を取り、`requirements.txt` を `==` で固定する。更新は Dependabot に任せて、上がったときに気づけるようにする。
参考: workspace/projects/ai-side-business が既に同じことをやっているので、そこから写すのが早い。
お手本: workspace/projects/ai-side-business の実装に合わせる。
該当: 固定率 0% / 依存 5 件

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 9a8299a05754 -n "理由"`
今後も出さない: `python -m growth dismiss 9a8299a05754 -n "理由"`
</details>

### 10. [中] 必要な環境変数の一覧が無い

- 対象: `nami0817kk-arch/workspace/projects/youtube-video-creation`
- 種別: ドキュメント
- なぜ: 時間が経つと自分でも「何を設定すれば動くのか」が分からなくなる。
- やること: `.env.example` にキー名だけ（値は空）を並べる。README からそれを参照する。
参考: workspace が既に同じことをやっているので、そこから写すのが早い。
- 根拠: `お手本: nami0817kk-arch/workspace`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/youtube-video-creation の「必要な環境変数の一覧が無い」に対応してください。

背景: 時間が経つと自分でも「何を設定すれば動くのか」が分からなくなる。
やること: `.env.example` にキー名だけ（値は空）を並べる。README からそれを参照する。
参考: workspace が既に同じことをやっているので、そこから写すのが早い。
お手本: workspace の実装に合わせる。

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze 960a26d669ae -n "理由"`
今後も出さない: `python -m growth dismiss 960a26d669ae -n "理由"`
</details>

### 11. [中] 1つの関数が長くなりすぎている

- 対象: `nami0817kk-arch/workspace/projects/youtube-video-creation`
- 種別: コード品質
- なぜ: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
- やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
- 根拠: `src/cli.py:165 main() 151行` / `src/cli.py:319 _dispatch() 1269行`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace/projects/youtube-video-creation の「1つの関数が長くなりすぎている」に対応してください。

背景: 100行を超える関数は、頭に入れながら読むのが難しい。変更のたびに全体を追い直すことになり、テストも書きづらい。
やること: 関数の中で「まとまった仕事」をしている塊を、名前を付けて切り出す。切り出した先はテストしやすくなるので、そこから1本書ける。
該当: src/cli.py:165 main() 151行 / src/cli.py:319 _dispatch() 1269行

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze b3e2f8965caf -n "理由"`
今後も出さない: `python -m growth dismiss b3e2f8965caf -n "理由"`
</details>

### 12. [低] ドキュメント内のリンク先が存在しない

- 対象: `nami0817kk-arch/workspace`
- 種別: ドキュメント
- なぜ: リンクをたどった先が無いと、書いてある内容自体が信用されなくなる。
- やること: リンク先を直すか、まだ無いものへのリンクなら文章から外す。
- 根拠: `platform/ai-lab/site/content/guides/ai-cost-overview.md -> /guides/automation-running-cost/` / `platform/ai-lab/site/content/guides/ai-cost-overview.md -> /guides/build-the-business-case/` / `platform/ai-lab/site/content/guides/ai-cost-overview.md -> /guides/japanese-token-ratio/` / `platform/ai-lab/site/content/guides/ai-cost-overview.md -> /guides/llm-cost-basics/` / `platform/ai-lab/site/content/guides/ai-cost-overview.md -> /tools/automation-roi/` / `platform/ai-lab/site/content/guides/ai-cost-overview.md -> /tools/hourly-rate/`

<details><summary>Claude にそのまま貼る依頼文</summary>

```
nami0817kk-arch/workspace の「ドキュメント内のリンク先が存在しない」に対応してください。

背景: リンクをたどった先が無いと、書いてある内容自体が信用されなくなる。
やること: リンク先を直すか、まだ無いものへのリンクなら文章から外す。
該当: platform/ai-lab/site/content/guides/ai-cost-overview.md -> /guides/automation-running-cost/ / platform/ai-lab/site/content/guides/ai-cost-overview.md -> /guides/build-the-business-case/ / platform/ai-lab/site/content/guides/ai-cost-overview.md -> /guides/japanese-token-ratio/ / platform/ai-lab/site/content/guides/ai-cost-overview.md -> /guides/llm-cost-basics/ / platform/ai-lab/site/content/guides/ai-cost-overview.md -> /tools/automation-roi/

小さく1コミットにまとめ、なぜその変更が要るかを README かコミットメッセージに一行残してください。
```

今はやらない: `python -m growth snooze e267e04f7b46 -n "理由"`
今後も出さない: `python -m growth dismiss e267e04f7b46 -n "理由"`
</details>

## 保留中（理由あり）

見たうえで今はやらないと判断したもの。状況が変われば `growth run` の次回で戻せる。

- `workspace/projects/cohabitation-budget` 雛形だけ作られて中身が無い — index.html 1枚で完結させることが価値の完成品。README に「完成・運用中／構成を分割しない」と明記済み

## 見送った分

1回あたりの提案数の上限に達したため、6 件は次回以降に回した。

- `workspace/libs/kabutan` .env.example で必要な設定を明示する（開発モノレポ（全PJT） で既に実践中）（低）
- `workspace/projects/kabu-agari-ranking` ドキュメントが存在しないファイルを載せている（低）
- `workspace/projects/soccer-manager` ドキュメントが存在しないファイルを載せている（低）
- `workspace/projects/soccer-manager` 1ファイルが大きくなりすぎている（低）
- `workspace/projects/tool-factory` ドキュメントが存在しないファイルを載せている（低）
- `workspace/projects/tool-factory` .env.example で必要な設定を明示する（開発モノレポ（全PJT） で既に実践中）（低）

## プロジェクト別の成熟度

| プロジェクト | 種別 | 成熟度 | 前回比 | 未対応 |
|---|---|---:|---:|---:|
| `workspace` | monorepo | 100 | ±0 | 2 |
| `workspace/projects/ai-side-business` | Python | 100 | ±0 | 1 |
| `workspace/projects/ir-analysis` | Python | 100 | ±0 | 0 |
| `workspace/projects/kabu-agari-ranking` | Python | 100 | ±0 | 1 |
| `workspace/projects/price-tracker` | Python | 100 | ±0 | 0 |
| `workspace/projects/quality-gainer-tracker` | Python | 100 | ±0 | 2 |
| `workspace/projects/stock-investment` | Python | 100 | ±0 | 3 |
| `workspace/projects/tool-factory` | Python | 100 | ±0 | 2 |
| `workspace/projects/youtube-video-creation` | Python | 100 | ±0 | 3 |
| `workspace/projects/soccer-manager` | Flutter | 95 | ±0 | 2 |
| `workspace/libs/kabutan` | Python | 95 | ±0 | 1 |
| `workspace/projects/gemini-api` | Python | 93 | ±0 | 1 |
| `workspace/projects/cohabitation-budget` | 雛形のみ | 15 | ±0 | 0 |
