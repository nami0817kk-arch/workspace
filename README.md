# cohabitation-budget — 同棲費用シミュレーター

同棲の初期費用・毎月の生活費を入力して、二人での分担額を計算する1ページのツール。

**`index.html` を開くだけで動く。** ビルドもサーバも依存パッケージも無い。
入力はブラウザの localStorage に保存されるので、開き直しても消えない
（保存されるのは自分のブラウザの中だけ）。

## 状態

**完成・運用中。** 機能追加の予定は無い。
1ページで完結させることがこのツールの価値なので、構成を分割しない。
