# CI（Linux）でテストを動かすためだけの最小セット。バージョンは固定する。
#
# 本体の requirements.txt には pywin32 / pyodbc といった Windows 前提の
# パッケージが入っていて、Linux ランナーではインストールできない。
# テストはそれらに依存しない部分だけを対象にしているので、ここを分けている。
pandas==3.0.5
ta==0.11.0
yfinance==1.7.0
pytest==9.1.1

# src/report/news.py を import するために必要（いずれも Linux で入る）。
# 例外経路のテストを「skip されるだけの空テスト」にしないため、ここに入れている。
# バージョンは requirements.txt の下限に合わせて固定。
feedparser==6.0.14
anthropic==1.2.0
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.2

# src/report/excel_exporter.py を import するために必要。
# excel_exporter は db_manager 経由で pyodbc に依存する。pyodbc は Linux でも
# wheel が入り import できる（quality-gainer-tracker の CI で実証済み）。
# 冒頭のコメントにある「Linux ランナーではインストールできない」は
# pywin32 についてのみ当てはまる。
openpyxl==3.1.5
pyodbc==5.3.0
