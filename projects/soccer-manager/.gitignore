# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# 署名・配布用の秘密情報 (絶対にコミットしない)
# Android
android/key.properties
**/*.jks
**/*.keystore
# iOS / Apple
**/*.p12
**/*.p8
**/*.cer
**/*.certSigningRequest
**/*.mobileprovision
**/*.provisionprofile
# 秘密鍵そのもの。RELEASE_GUIDE の手順で作られる
# ios_distribution.key / distribution.pem / ios_distribution.csr を含む。
**/*.key
**/*.pem
**/*.csr

# CI へ渡すために Base64 化したもの。
# **中身は鍵そのもの**で、テキストになっただけ。手順書がこのファイル名で
# 作らせるのに除外されておらず、リポジトリに入りうる状態だった。
**/*.base64.txt
keystore.base64.txt
ios_cert.base64.txt
ios_profile.base64.txt
appstore_api_key.base64.txt
ios/Runner/GoogleService-Info.plist
# ストア API 資格情報
**/play-store-service-account*.json
**/AuthKey_*.p8


# gen-l10n が出す未翻訳レポート。生成物なので追跡しない。
l10n_untranslated.json
