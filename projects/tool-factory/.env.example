# このプロジェクトが読む環境変数は無い。
#
# サイト名・base_url・広告ID・計測IDは、環境変数ではなく site.json に置く。
# 「.env が要るのに例が無いのか」を毎回確かめずに済むよう、
# 無いことを明示するためだけに置いてある。
#
# 公開に使う Cloudflare の認証情報（CLOUDFLARE_API_TOKEN /
# CLOUDFLARE_ACCOUNT_ID）は手元ではなく GitHub Secrets に置く。
