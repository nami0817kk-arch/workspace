# X(Twitter) への自動投稿に使う認証情報。
# 値はここに書かず、ローカルは .env、CI は GitHub Secrets に入れる。
# 未設定でもサイトのビルドは通る（投稿だけがスキップされる）。
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=

# Cloudflare Pages へのデプロイに使う。CI 専用で、ローカルには不要。
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
