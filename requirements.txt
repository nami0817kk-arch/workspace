# 運用PC（システムPython）の実測値で固定。`>=` に緩めない。
# 更新は Dependabot の PR で受け取り、CI が通るのを見てから上げる。
yfinance==1.7.0
pandas==3.0.5
ta==0.11.0
python-dotenv==1.2.2
pyodbc==5.3.0
pywin32==312; sys_platform == "win32"
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.2

# kabutan の取得・解析は ai-lab の共有パッケージに一本化してある
# （kabu-agari-ranking と共通。HTML 構造の変化はここを更新して直す）。
ai-lab[kabutan] @ git+https://github.com/nami0817kk-arch/ai-lab.git@fba472d6d64ac6e78361f6e4a9bc63fa6d32f0a4
