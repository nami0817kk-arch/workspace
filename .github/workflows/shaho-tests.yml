name: shaho tests

on:
  push:
    branches: ["**"]
    paths: ["projects/shaho-tekiyo/**", ".github/workflows/shaho-tests.yml", ".github/workflows/python-tests.yml"]
  pull_request:
    paths: ["projects/shaho-tekiyo/**", ".github/workflows/shaho-tests.yml", ".github/workflows/python-tests.yml"]
  workflow_dispatch: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  pytest:
    uses: ./.github/workflows/python-tests.yml
    with:
      workdir: "projects/shaho-tekiyo"
      cache-dependency-path: "projects/shaho-tekiyo/requirements.txt"
