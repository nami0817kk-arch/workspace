name: CI

# コードの検証だけを行うワークフロー。公開(デプロイ)は soccer-pages.yml が持つ。
# 分けてあるのは、公開先の設定ミスでビルドとテストの結果まで赤くなると、
# 壊れているのがコードなのか公開の設定なのか判断できなくなるため。
#
# paths-ignore は意図的に置いていない。distribution_test.dart が
# STORE_LISTING.md や legal/*.html を読んで整合性を検査しているので、
# 「ドキュメントだけの変更」でも検証は回す必要がある。

on:
  push:
    branches:
      - master
      - main
    paths:
      - "projects/soccer-manager/**"
      - ".github/workflows/soccer-ci.yml"
  pull_request:
    paths:
      - "projects/soccer-manager/**"
      - ".github/workflows/soccer-ci.yml"
  # 手で回すとき用。テストの反復はローカルではなくここで行う方針のため
  # (docs/session-faq.md「テストはクラウドで回す」)、対象を絞れるようにして
  # ある。1ファイルだけ直しては回す、という使い方で全件を待たずに済む。
  workflow_dispatch:
    inputs:
      target:
        description: "回すテスト(例 test/game_logic_test.dart)。空なら全件"
        required: false
        default: ""
      skip_analyze:
        description: "analyze を省く(テストの反復だけしたいとき)"
        type: boolean
        default: false

# 同じブランチで新しい実行が始まったら、古い方は打ち切る。
# 連続で push したとき、途中の版のテストに時間を使わないため。
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: projects/soccer-manager
    # ハングしたジョブが既定の6時間走り続けるのを防ぐ
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Install dependencies
        run: flutter pub get

      # 静的解析。手で対象を絞って回すときだけ省ける。push/PR では必ず走る。
      - name: Analyze
        if: ${{ inputs.skip_analyze != true }}
        run: flutter analyze

      # 未翻訳・字形の検査は python を呼ぶ。ubuntu-latest には入っている。
      - name: Run tests
        env:
          # 入力を run の中へ直接展開しない(スクリプト注入を避ける)。
          TARGET: ${{ inputs.target }}
        run: |
          if [ -z "$TARGET" ]; then
            flutter test
            exit $?
          fi
          # 素直な test/ 配下のパスだけ通す。
          case "$TARGET" in
            test|test/|test/*) ;;
            *)
              echo "::error::target は test/ 配下を指定してください: $TARGET"
              exit 1 ;;
          esac
          case "$TARGET" in
            *[!A-Za-z0-9_/.-]*)
              echo "::error::target に使えない文字が含まれています: $TARGET"
              exit 1 ;;
          esac
          echo "対象を絞って実行: $TARGET"
          flutter test "$TARGET"
