name: price-tracker tests

on:
  push:
    branches: ["**"]
    paths: ["projects/price-tracker/**", ".github/workflows/price-tracker-tests.yml"]
  pull_request:
    paths: ["projects/price-tracker/**", ".github/workflows/price-tracker-tests.yml"]
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  pytest:
    uses: ./.github/workflows/python-tests.yml
    with:
      workdir: "projects/price-tracker"
      install: "pip install pytest"
